# AGENTS.md — FK Cēsis MMS

*Authoritative project guide. Keep this file updated after major changes.*

## Project Purpose
Django MVP for FK Cēsis youth football club: parent registration, admin approval, secure identity-document handling, and Invoice Ninja billing orchestration.

## Stack
- **Python 3.12+**, **Django 5.x**, **PostgreSQL** (psycopg)
- **uv** for dependency management and script execution
- **pytest** + **pytest-django**, **ruff**, **mypy**
- Private file/object storage, background job runner (Celery / Django-Q)
- Server-rendered templates (parent + admin), minimal JS

## Architecture
Target Django monolith with domain apps:
- `apps/core` — shared base models, enums, audit helpers
- `apps/accounts` — ParentAccount, magic-link auth
- `apps/registrations` — RegistrationApplication workflow, OCR intake
- `apps/members` — Member, Guardian, TrainingGroup
- `apps/billing` — MembershipPlan, sibling discount, Invoice Ninja sync
- `apps/documents` — private Document model, audited access views
- `apps/integrations` — Invoice Ninja / OCR clients, retry state
- `apps/admin_ops` — admin dashboards, CSV export *(planned, not yet implemented)*

## Current Status
**P1 + P2 are complete, P3 is signed off (live validation evidence in `docs/p3_tiny_idp_validation.md`), P4 is complete, P5 Slices A–D are delivered (full approval-to-agreement flow including DocuSeal-backed e-signature), P6 Slices A–C + installment-calendar + guardian-identity Slices A–C are delivered (local billing domain + sibling-discount engine, admin-confirmed Invoice Ninja push, payment read-back, installment-calendar with skip-months/due-day, and the full canonical Guardian 1:1 dedup: read-through, dropped columns, locked-profile UX, and admin email change), P7 is complete (admin review consolidation, CSV export, audit baseline, admin polish), P8 is complete (agreement lifecycle: minor/material amendment, discontinuation, credit-note flow with Invoice Ninja live validation), P9 is complete (billing plan lifecycle: explicit `Agreement.billing_plan` + `first_billing_month` snapshot, default plan preselection, renewal flow, draft-only reassignment; signing guard refuses to mutate state without a billing plan), P10 is complete (privacy-aware parent-flow analytics + registration funnel with Plausible-first adapter, referral-code attribution, parent-only browser hooks, and server milestone events).** Registration workflow is usable for LAN acceptance testing; admin review queue, member creation baseline, guardian-email-first verified registration gate, OCR-backed parent/admin review flow, the agreement lifecycle (generate → sent → DocuSeal-signed/manual, with void + regenerate), signing-triggered draft billing records, the admin-confirmed Invoice Ninja invoice push, payment read-back, canonical guardian deduplication (one Guardian per ParentAccount, sibling-discount linkage correct), and explicit billing-plan intent (default plan preselection + admin-set billing setup + selected-member renewal + draft reassignment) are operational.
- Django project scaffold exists and boots.
- `apps/` package exists with app configs for `core`, `accounts`, `registrations`, `members`, `billing`, `documents`, `integrations`.
- `apps/core/models.py` includes abstract `TimeStampedModel`.
- `apps/accounts/models.py` implements `ParentAccount`, `MagicLinkToken`, and `EmailVerificationCode`.
- `apps/accounts/services.py` implements `issue_magic_link`, `send_magic_link`, `consume_magic_link`, plus one-time email code issue/send/verify helpers.
- `apps/accounts/views.py` implements request, verify, logout, and one-time code verification views.
- `apps/accounts/management/commands/ensure_admin_user.py` for env-driven admin creation.
- `apps/registrations/models.py` implements `RegistrationApplication` with finalized P1 guardian/member/application fields, draft/submitted states, and fix/reject/approve workflow.
- `apps/registrations/services.py` implements application lifecycle: create, save draft, submit, chooser/prefill support, same-address handling, link to parent account, admin review actions (request_fix, reject, approve), OCR-triggered identity upload processing, guardian-doc reuse, and OCR-derived prefill/field-source mapping. `fix_requested` save preserves status (Task 3).
- `apps/registrations/views.py` provides guardian email entry, verified registration create/edit, chooser portal, admin review queue/detail views, canonical application workspace routing, parent OCR summary rendering, and admin OCR review context.
- `apps/registrations/presentation.py` implements grouped form rendering contract and workspace mode logic (Task 3).
- `apps/registrations/forms.py` implements the unified registration form with grouped sections (Task 3).
- `apps/registrations/templatetags/reg_filters.py` provides template filters for the form contract (Task 3).
- `apps/members/models.py` implements `Member`, `Guardian`, `TrainingGroup`, and `KitSizeOption` models; approval creates `Member` + `Guardian` with `training_group` left empty.
- `apps/documents/models.py` implements `Document` model with private storage (`PRIVATE_DOCUMENTS_ROOT`), OCR process state fields, and `DocumentExtraction` for encrypted OCR payload/summary persistence.
- `apps/documents` uses a dedicated private storage root (`private-uploads/`), Fernet-encrypted OCR payload/summary helpers in `apps/documents/ocr.py`, and admin-only protected preview/download endpoints (`/admin/documents/<id>/preview/`, `/admin/documents/<id>/download/`). Anonymous users are redirected to admin login; non-admin authenticated users receive `404`.
- `.env` autoload works for local commands and app startup.
- Address autocomplete now has a weekly URL-backed VZD VARIS import with `AW_DZIV` apartment suggestions, suspicious-drop protection, and read-only import-run admin visibility.
- Current acceptance testing runs on LAN URL `http://192.168.3.245:8000`.

### P1 delivered registration workflow UX
- `/register/` is guardian email entry for one-time code verification.
- `/register/verify/` completes verified parent access before continuation.
- `/portal/` acts as chooser/dashboard for verified guardians.
- `/applications/new/` starts a new verified registration with guardian-only prefill.
- `/applications/<id>/` is the canonical parent application workspace (Task 3); legacy parent routes redirect here.
- Anonymous same-browser draft continuation was removed; edit/submit now require verified parent ownership.
- Edit page uses a single form with two actions: **save draft** and **submit application**.
- Member address supports live **Adrese tāda pati kā vecāka** sync and restore behavior.
- Grouped form rendering contract in place (Task 3): guardian, child/player, and document sections rendered via shared template primitives (`form_field.html`, `source_badge.html`).
- Application workspace supports read-only (submitted/approved/rejected) and editable (draft/fix_requested) modes; `fix_requested` save preserves status (Task 3).

### Task 4 delivered — document state, OCR source cues, error summary
- `templates/parent_ui/includes/document_card.html` — reusable document card partial showing filename, kind label, active/not-uploaded state, and replace/upload links for parent workspace.
- `templates/parent_ui/includes/error_summary.html` — updated to render field label, validation message, and anchor link to invalid field via `items` parameter.
- `templates/registrations/application_workspace.html` — includes document card section in both editable and read-only modes; replace/upload links only shown in editable mode; passes source labels to all form fields.
- Source badges render for `manual_only`, `derived_system_filled`, and OCR markers (`ocr_guardian_identity`, `ocr_member_identity`) using `SOURCE_LABEL_MAP` in `presentation.py`.
- Invalid-submit error summary shows heading, field label, validation message, and anchor target (`id-guardian_email` pattern).
- No schema changes, no business rule changes, no admin redesign.

### P2 delivered — visual system refinements, document-state/review-cue presentation
- Typography refined for readability (desktop and mobile).
- Active uploaded-document state and replace guidance clarified via document card partial.
- Review/correction cues completed at presentation layer without real OCR dependency: source badges, error summary with anchor links, invalid-submit error summary.
- No schema changes, no business rule changes, no admin redesign.

### P3 delivered — OCR integration + secure extracted metadata baseline
- `apps/integrations/ocr.py` provides OCR provider boundary with deterministic stub mode and tiny-IDP hook point.
- Identity uploads for `guardian_identity` and `member_identity` now run synchronous OCR in draft flow; `member_portrait` stays outside OCR scope.
- OCR success persists encrypted payload and encrypted summary in `DocumentExtraction`; OCR failure stays non-blocking and records failed state.
- `/applications/new/` reuses active guardian identity document by default for returning verified guardians and merges prior OCR extraction into new-app prefill.
- Parent workspace shows OCR-derived source labels and decrypted OCR summaries for uploaded identity documents.
- Admin review detail shows separate guardian/member document preview sections, decrypted OCR summaries, and confidence values when provider returns them.
- Test-client file upload workaround in `tests/conftest.py` supports Django 6 multipart posts with `files=`.
- Full repo verification after P3 landing: `uv run pytest -q` → `584 passed`, `uv run ruff check .` → passed, `uv run mypy .` → passed.
- Classified exception mapping in `safe_extract_document_data`: `_classify_exception()` maps typed OCR errors (`provider_misconfigured`, `auth_failed`, `rate_limited`, `request_timeout`, `provider_unavailable`, `invalid_response`) to `Document.ocr_error_code` for admin review. Unknown exceptions fall back to `provider_unavailable`.
- Real tiny-IDP runtime is landed; `TINY_IDP_API_URL` and `TINY_IDP_API_KEY` are the canonical config names. `OCR_ENCRYPTION_KEY` is required for OCR payload/summary encryption.
- Live sample-document validation evidence captured in `docs/p3_tiny_idp_validation.md` (run via `uv run python -m scripts.validate_tiny_idp`). P3 signed off 2026-05-22.
- Live validation surfaced and fixed three integration bugs against the real `api.tiny-idp.com` generic-id-document API: (1) auth header is `x-api-key`, not `Authorization: Bearer`; (2) multipart field name is `files`, not `file`; (3) response is `{success, data: {...}, balance, cost}` — adapter and tests had been built against a fictional `{entities, document, confidence}` shape. Normalizer now maps `data.given_names`→`first_name`, `data.first_surname`→`last_name`, `data.personal_number`→`personal_id`, `data.issuing_authority`→`issuer`, `data.issuing_date`→`issuance_date`, `data.date_of_birth`→`date_of_birth`, and derives `confidence` from `*_verified` booleans. Auth failure with malformed key returns HTTP 200 + `{"code": "API_KEY_REQUIRED"}` body, which `post_document` now classifies as `AuthError`.

### P4 Slice A delivered — foundations (2026-05-23)
- `apps/integrations/name_normalization.py` — pure Latvian title-case helper applied at OCR consumption boundaries only: the `encrypted_summary` builder in `apps/integrations/tasks.py` and the `_merge_ocr_extractions` prefill reads in `apps/registrations/services.py`. The encrypted OCR payload at rest stays raw (audit posture preserved). Handles hyphenated compounds, nobiliary particles (lowercase unless sole token), and Latvian diacritics; defensive against non-string input. Covered by 18 parametrized cases in `tests/integrations/test_name_normalization.py`.
- `RegistrationApplication.personal_data_consent_at` + `personal_data_consent_version` fields (migration `0007_personal_data_consent`, both nullable). Current consent version constant: `apps.registrations.models.PERSONAL_DATA_CONSENT_VERSION = "v1-2026-05"`. Both fields are listed in `RegistrationApplicationAdmin.readonly_fields` so admin staff cannot stamp or unstamp consent directly. Gate UX, T&C partial, and consent persistence on submit land in P4 Slice C.
- Cross-cutting parent-UI primitives in `templates/parent_ui/includes/`: `spinner.html` (`data-spinner`, `role="status"`), `toast.html` (`data-toast`, `data-toast-tone`; `warning` tone uses `role="alert"` + `aria-live="assertive"`), `empty_state.html` (`data-empty-state`), `error_state.html` (`data-error-state`, `role="alert"`). All follow the existing `fk-*` BEM-style class prefix. Consumers (visibility-aware polling, OCR-done toast, entry/portal empty + error states) land in P4 Slices B–E.

### P4 Slice B delivered — P3.5 polish leftovers (OCR UX) (2026-05-23)
- `apps/integrations/ocr_messages.py` — centralized Latvian error-code → message map plus generic fallback (`get_ocr_error_message`). Six known codes covered (`provider_misconfigured`, `auth_failed`, `rate_limited`, `request_timeout`, `provider_unavailable`, `invalid_response`); unknown / missing codes fall back to a generic "fill manually" message. No English fallback anywhere in the parent flow.
- Status polling endpoint (`/applications/<id>/documents/<doc_id>/status/`) now emits `ocr_error_message` on FAILED responses alongside the existing `ocr_error_code`. Running / completed payloads do not carry it.
- `static/js/async_upload.js` no longer renders raw text status. Running state uses the `fk-spinner` markup; completed state uses an auto-dismissing `fk-toast` carrying `"Dokumenta apstrāde pabeigta. Persona atpazīta kā <Name>."` (name comes pre-normalized from Slice A); failed state renders the server-supplied Latvian message. Polling pauses when `document.hidden === true` and resumes on a one-shot `visibilitychange` listener.
- `static/css/parent_theme.css` ships full styling for `.fk-spinner`, `.fk-toast` (+ success / warning variants), and the previously-unstyled `.fk-ocr-suggestion` chip family. `prefers-reduced-motion` disables the spinner animation.

### P4 Slice C delivered — wizard gating + auto-save + consent gate (2026-05-24)
- Consent persistence is stamped server-side only. `apps.registrations.services.create_or_update_draft` writes `personal_data_consent_at` (now) and `personal_data_consent_version` (`apps.registrations.models.PERSONAL_DATA_CONSENT_VERSION = "v1-2026-05"`) when the form supplies `personal_data_consent=True`. Re-stamp rule: stamp on first True, AND when the stored version mismatches the current constant; once set, the fields are never cleared. The template renders the checkbox pre-checked only when stored version matches current AND timestamp is non-null.
- `apps/registrations/templates/registrations/_terms_v1.html` ships default Latvian T&C text marked "pending legal review" via a `{% comment %}` header. Production cutover requires legal sign-off, and any material text change must bump `PERSONAL_DATA_CONSENT_VERSION` so consent re-stamps on next save.
- Auto-save endpoint contract on `registrations:application-workspace` branches on `X-Requested-With: XMLHttpRequest`: success (200) returns `{"saved_at": "<ISO>", "consent_recorded": <bool>}`; form invalid (400) returns `{"errors": [{"field", "label", "message"}, ...]}`; service `ValueError` (400) returns `{"errors": [{"field": "__all__", "label": "", "message": "<exc>"}]}`; non-editable applications still raise Http404. AJAX POSTs never submit even if `submit_action=submit` is in the payload — auto-save can never accidentally graduate a draft.
- Wizard step-gating contract: inputs in `submit_required_fields` (plus the two document inputs) carry `data-step-required="<step-name>"` and `data-step-error-empty="<lv-msg>"` (and `data-step-error-format="<lv-msg>"` for `*_personal_id` inputs). `static/js/wizard.js` reads these attrs, validates on blur/change/input, and toggles `[data-wizard-next].disabled` accordingly. `member_actual_address` is skipped when `member_same_address_as_guardian` is checked, mirroring the server-side waiver in `forms.py::clean()`. The consent checkbox participates in the step-1 gate via `data-step-required="documents"` + `data-personal-data-consent`.
- `apps/registrations/messages.py` holds wizard / consent / save-indicator Latvian strings as plain top-level `str` constants. The template injects them via `data-step-error-empty`, `data-step-error-format`, `data-save-message-saving|saved|error` attributes — `wizard.js` reads from the DOM, never hardcodes Latvian.

### P4 Slice D delivered — camera capture + mobile-first workspace (2026-05-25)
- `templates/parent_ui/includes/document_card.html` now owns the upload UI for every document kind (guardian ID, member ID, member portrait). Each card renders a single canonical hidden `<input type="file">` (visually-hidden via `.fk-visually-hidden`) plus two visible `<label for="id_<field>">` controls: "Augšupielādēt failu" (native label→input wiring) and "Uzņemt attēlu" (carries `data-camera-affordance`, lives inside `.fk-camera-only`).
- `member_portrait_document` moved from the member section into the documents section in `RegistrationApplicationForm.section_order` so all three document uploads live in step 1.
- `static/js/async_upload.js` ships a camera-affordance shim: clicking a `label[data-camera-affordance]` calls `event.preventDefault()`, sets `accept="image/*"` and `capture="environment"` on the canonical input, then calls `input.click()`. A `pointerdown` listener on the non-camera file labels clears stale `capture` attrs (handles the camera-cancelled-without-pick edge case).
- `.fk-camera-only` is hidden on non-coarse pointers via `@media not (pointer: coarse)` — no JS feature detection, no UA sniffing. `wizard.js` step-gating is untouched (single canonical input per slot).
- Mobile-first polish: `.fk-wizard-nav--sticky` modifier on per-step nav rows turns sticky on `(pointer: coarse) and (max-width: 720px)`; container padding uses `clamp(16px, 4vw, 32px)`; `.fk-address-row` wraps the actual-address + same-as-guardian fields and stacks at `max-width: 720px`; `.fk-save-indicator` switches to `position: static` on mobile so it doesn't collide with the sticky CTA.
- Touch-target floors enforced at class level: `.fk-button` already ≥48 px; `.fk-button--small` bumped to 40 px; date inputs covered by the existing `.fk-form-field input[type="date"]` 46 px rule. `.fk-checkbox-row` wrapper (≥44 px, 10 px vertical padding) is available for consent + address-sync rows. Hidden file inputs are exempted (tap surface lives on the visible labels).
- Defensive cleanup: `apps/registrations/templatetags/reg_filters.py::get_item` now returns `None` for non-dict input (was AttributeError), so partials referencing context that lands in a later task render harmlessly during the transition.
- `apps/registrations/presentation.py` exposes a new public `FIELD_NAME_BY_KIND` constant (inverse of the existing private `_FIELD_TO_KIND`) consumed by the view to build `document_bound_fields` for the partial.
- Workspace-template internal class rename: `fk-wizard-nav-actions` → `fk-wizard-actions` to avoid substring collision with `fk-wizard-nav--sticky` in test regexes. Touched `parent_theme.css`, `application_workspace.html`, `new_registration.html`. No JS consumers; no template consumers outside the two renamed files.
- Each upload label carries a decorative Feather-style inline SVG icon (upload-arrow on the file label, camera on the camera label). Icons are `aria-hidden="true"` so screen readers read only the Latvian text. `.fk-button--full` (width 100% + `box-sizing: border-box`) stretches both labels equally inside the `.fk-upload-slot` flex column.
- Post-merge fix: multi-line `{# ... #}` template comments in `templates/parent_ui/includes/{document_card,save_indicator,toast,error_state,empty_state}.html` were leaking their bodies into the rendered page (Django's `{# #}` is single-line only). Converted to `{% comment %}...{% endcomment %}` (or deleted where the comment was task-history scaffolding). Regression test in `tests/registrations/test_application_workspace_template.py::TestSliceDWorkspaceTemplate.test_no_leaked_django_comment_markers` fails whenever a literal `{#` appears in the rendered workspace HTML.
- Full repo verification after Slice D landing: `uv run pytest -q` → `798 passed`, `uv run ruff check .` → passed, `uv run mypy .` → passed (143 source files).
- Manual LAN verification on `http://192.168.3.245:8000/applications/<id>/` complete (2026-05-25). Phone: 3 cards with two buttons each; "Uzņemt attēlu" opens the device camera; "Augšupielādēt failu" opens the file picker; sticky "Turpināt →" stays visible while scrolling; OCR toast still appears after upload; address-sync row stacks on step 2. Desktop: camera buttons hidden; inline wizard nav; stepper-click navigation works. Initial render exposed two template-comment leaks and unequal button widths — fixed in follow-up commits (see entries above).

- **P4 Slice E delivered (2026-05-25):** Closeout polish for the parent-flow entry/chooser/portal surfaces and Latvian copy normalization across all parent-facing pages.
  - `/register/`: removed duplicate `fk-section-card` include, repaired the broken `fk-button primary` class, dropped the `fk-eyebrow` wrapper, added `inputmode="email"` + `autocomplete="email"`, full-width primary CTA.
  - `/register/verify/`: switched the pending-email notice to the new `.fk-page-intro` helper, added `inputmode="numeric"` / `autocomplete="one-time-code"` / `autofocus` to the code input, full-width CTA.
  - `/portal/`: replaced the bespoke `<div class="fk-empty-state">` markup with the shared `empty_state.html` partial (now accepts optional `cta_url`/`cta_label`); stripped inline `style="margin-top:…"` attributes inside the application-card region in favour of a `.fk-app-meta--review` modifier; action anchors and helper-card CTA gain `fk-button--full`. Dynamic `<span style="width:N%">` widths inside `.fk-progress-bar` preserved (encode content, not styling).
  - CSS: `.fk-page-intro` helper added; new `@media (max-width: 720px)` block stacks `.fk-applications`, `.fk-application-card`, `.fk-app-actions .fk-button`, `.fk-helper-card` for the mobile baseline.
  - Latvian copy: new regression test `tests/registrations/test_parent_surface_copy_contract.py` scans rendered visible text on `/register/`, `/register/verify/`, `/portal/` (empty + with apps), `/applications/<id>/`, plus a static scan of `new_registration.html`, for English-token leakage. Token list fixed; legitimate fragments allowlisted in code with comments. Initial sweep found no English leaks (codebase was already Latvian-by-default; three Latvian-phrase guards were added to absorb the Latvian preposition "no").
  - Manual LAN verification on 192.168.3.245 (2026-05-25):
    - Slice E surfaces (`/register/`, `/register/verify/`, `/portal/`) rendered correctly at narrow viewports with full-width CTAs and no English-text leakage.
    - LAN check also surfaced two regressions adjacent to Slice E that were fixed in follow-up commits (see below) and six pre-existing bugs that are out of Slice E scope and have been queued as P4.5 (see `docs/milestones.md`).
  - Slice E LAN follow-up fixes (also 2026-05-25):
    - **Wizard CTA cut off on mobile (commits `80b3109`, `3d849cd`):** `.fk-wizard-nav .fk-button` fills its grid track (`width: 100%; box-sizing: border-box; padding: 0 14px; font-size: 14px`); `.fk-wizard-actions` overridden to `grid-template-columns: 1fr` at 720 px. Regression tests added under `TestParentThemeCssPortalMobile`.
    - **OCR summary rendered raw field IDs (commit `8351c2a`):** added `OCR_FIELD_LABELS` and `parse_ocr_summary` in `apps/registrations/presentation.py`; the workspace view now passes structured `(label, value)` tuples; the template renders a Latvian-labeled `<dl>` with the kind label resolved via `document_kind_labels`. Encryption/storage unchanged. New test file `tests/registrations/test_ocr_summary_presentation.py` covers helper + rendered DOM + CSS contract. One existing assertion in `test_workspace_ocr_decryption.py` was updated to match the new presentation contract.
    - **`member_same_address_as_guardian` checkbox blocked step advance (commit `fa80c45`):** removed field from `_field_step_map` in `RegistrationForm.__init__`; wizard treats unchecked checkbox as a valid (unfilled) optional field. New test file `tests/registrations/test_member_same_address_checkbox.py`.
    - **Document kind labels shown in English (commit `3d849cd`):** `DOCUMENT_KIND_LABELS` in `presentation.py` now contains explicit Latvian strings; `document_card.html` simplified to always use the map. New test file `tests/registrations/test_document_kind_labels.py`.
    - **Review step showed redundant "Saglabāt melnrakstu" button (commits `6732f4b`, `fb16bb1`):** removed save_draft button and `.fk-wizard-actions` wrapper from review step in both `application_workspace.html` and `new_registration.html`; "Iesniegt pieteikumu" now uses `fk-button--primary fk-button--full` matching the Turpināt style. New test file `tests/registrations/test_review_step_submit.py` (5 tests).
  - Test suite: 847 passed (up from 798 baseline); ruff and mypy clean.

- **M6 containerization + two-channel deploy pipeline landed (2026-05-26)**
  - Multi-stage `Dockerfile` (uv builder → slim runtime; non-root UID 10001; `collectstatic` baked in; gunicorn + whitenoise; healthcheck `curl /healthz`).
  - `compose.yaml` with three services: `postgres` (Postgres 18 alpine, mount at parent `/var/lib/postgresql` for clean `pg_upgrade --link`, named `pgdata` volume), `web` (binds `127.0.0.1:${WEB_HOST_PORT:-8000}` on the host → 8000 in container, runs `migrate` then gunicorn, bind-mounts `./data/{uploads,private-uploads}`), `qcluster` (same image, `qcluster` command, waits for `web: service_healthy`, healthcheck disabled because it has no HTTP server). Container port is fixed at 8000; only the host-side port is configurable via `.env` to avoid collisions on the host.
  - `apps/core/views.py::healthz` + URL `/healthz` — returns 200 `{"status":"ok"}` after a `SELECT 1` against the DB; 503 on DB error. Covered by `tests/core/test_healthz.py`.
  - `fk_cesis_mms/settings.py` made env-driven for prod: `DJANGO_DEBUG` env, `DATABASE_URL` via `dj-database-url` (SQLite default for dev/tests), `DJANGO_ALLOWED_HOSTS` comma-merge, whitenoise middleware + `STATIC_ROOT = BASE_DIR / "staticfiles"`. Manifest storage activates only when `staticfiles.json` exists (i.e. container builds), so tests keep using Django's default storage.
  - New deps: `gunicorn`, `whitenoise`, `dj-database-url`, `psycopg[binary]`.
  - **Two-channel CI** (`.github/workflows/ci.yml`): lint + test on every push and PR. PRs run the fast test lane. Push to `dev` → build `ghcr.io/linards-kalvans/fk-cesis-mms:dev` (floating). Push to `main` → read `VERSION` (major), count commits since `VERSION` last changed (minor), build `:main` *and* `:<major>.<minor>` immutable. Deployment is manual for now; runtime ownership stays in `https://github.com/linards-kalvans/fk-cesis`.
  - **Versioning:** `VERSION` file at repo root holds `<major>` as an integer (currently `0`). `<minor>` resets to 1 the instant `VERSION` changes (commit count from the SHA where `VERSION` was last touched), then auto-increments on every commit/merge to `main`. Tags `:0.1`, `:0.2`, … are immutable in the registry; future `:1.1` arrives the moment `VERSION` is bumped to `1`.
  - `docs/deployment.md` is now an ownership pointer: deployed runtime lives in `https://github.com/linards-kalvans/fk-cesis`; this repo keeps the Docker image build/tag contract and local smoke compose only.
  - Local docker compose smoke verified twice: with default port → `postgres` healthy → `web` healthy (migrations + gunicorn) → `qcluster` healthy with clean log, `/healthz` 200, `/register/` renders Latvian title with `fk-*` classes; with `WEB_HOST_PORT=18000` → bind on `127.0.0.1:18000` works and `:8000` is correctly refused.
  - Full repo verification after the landing: `uv run pytest -q` → `858 passed`, `uv run ruff check .` → passed, `uv run mypy .` → passed.
  - Deployment runtime ownership moved to `https://github.com/linards-kalvans/fk-cesis` on 2026-06-27. This repo keeps the Docker image build/tag contract and local smoke compose only; server compose, Caddy, systemd, deploy listener, env templates, and rollout docs live in `fk-cesis`.

- **P4.5 delivered — parent-flow quality-debt sweep (2026-05-25)**
  - Guardian full-name no longer doubled in new-app prefill: `_merge_ocr_extractions` removed model-value fallback; OCR wins unconditionally (`commit 58115e1` + `0a80b4e`).
  - Member OCR fields no longer bleed into new-app prefill: member block removed from `_merge_ocr_extractions`; each new registration starts with a fresh child record. Two stale tests updated (`commit 15bcf86`).
  - OCR stub now emits `date_of_birth` in `person_fields` (member: `2015-06-15`, guardian: `1985-03-10`). `_ocr_extracted_fields` maps `date_of_birth` → `member_birth_date` for member identity docs so the workspace prefill path can populate the DOB field after OCR completes (`commit 77e7fc4`).
  - Document card DOM updates to "Aktīvs" state immediately after async upload: `markCardAsUploaded` in `async_upload.js` swaps badge + body and inserts the `data-uploaded-document` hook (`commit 38bab23`).
  - Step-gating now recognises pre-uploaded documents on draft resume: `document_card.html` emits `[data-uploaded-document]` when a doc is present; `wizard.js::isFileFieldFilled` reads it via `.fk-upload-slot` (`commit aca78bf`).
  - Mobile wizard: stepper clicks restricted to `visitedSteps` Set (prevents bypassing step gating by tapping ahead); `fk-step--visited` CSS class marks navigable steps; `.fk-step-number` bumped to 44 × 44 px for reliable touch targets (`commit e3a0d18`).
  - Guardian phone now stays in sync with the parent's most recent application: `submit_application` updates `account.phone` to match `application.guardian_phone` whenever they differ. `account.phone` remains the single source of truth for new-app prefill — no read-time overrides (`commits 1f23a7d`, `ba0b01a`).
  - Test suite: 856 passed; ruff and mypy clean.

- **P5 Slice A delivered — admin review: inline doc preview + approval-ready inspection (2026-05-27)**
  - Closes P5 acceptance items 1 + 2 only (training-group assignment, agreement generation, and DocuSeal sync remain queued for Slices B/C/D).
  - `apps/registrations/views.py::admin_review_detail` restructured: per-kind panel context (`guardian_panel`, `member_panel`, `portrait_panel`) built by a new `_build_doc_panel(application, kind, kind_label)` helper. Each panel carries `active`, `replaced` (all soft-deleted docs of that kind, ordered `-created_at`), `ocr_summary` (list of `(label, value)` tuples via `parse_ocr_summary`), `ocr_confidence_items` (list of `(label, value)` tuples through `OCR_FIELD_LABELS` — chips no longer leak raw English keys), `preview_kind`, `panel_title`, `kind`. Portrait kind never builds an OCR block.
  - `_doc_preview_kind(document)` module-level helper dispatches on the lower-cased extension of `original_filename` (fallback `file.name`): `jpg`/`jpeg`/`png`/`webp`/`heic` → `"image"`, `pdf` → `"pdf"`, anything else → `"other"`. Pure / parametrised-test-covered.
  - New partial `templates/registrations/admin/_doc_panel.html` renders one section per kind: inline `<img class="fk-doc-preview fk-doc-preview--image">` for image kinds, `<iframe class="fk-doc-preview fk-doc-preview--pdf" loading="lazy" height="600">` for PDFs, fallback `<p class="fk-doc-preview-fallback">` + download CTA for other types. OCR `<dl class="fk-ocr-readout">` and confidence `<ul class="fk-ocr-confidence">` render only when summary/confidence have content. Active doc carries `.fk-doc-badge--active`; replaced docs live inside `<details class="fk-doc-history">` and carry `.fk-doc-badge--replaced`. The `<details>` element is omitted entirely when there is no history.
  - `templates/registrations/admin_review_detail.html` rewritten: all inline `style="…"` attributes removed; review-action form keeps the same POST contract (`action=approve|request_fix|reject` + `review_message`) but the fix/reject disclosures are now native `<details>` instead of inline `onclick` JS; primary "Apstiprināt" button uses `fk-button fk-button--primary`.
  - CSS (`static/css/parent_theme.css` lines ~1234–1405): `.fk-doc-panel`, `.fk-doc-preview` + `--image` / `--pdf` modifiers, `.fk-doc-preview-fallback`, `.fk-doc-caption`, `.fk-doc-empty`, `.fk-doc-history`, `.fk-doc-badge` + `--active` / `--replaced`, `.fk-ocr-readout`, `.fk-ocr-confidence`, `.fk-review-actions`. No new `--fk-*` token variables introduced; rgba literals used in the Slice A section to satisfy the in-repo CSS regression test that bans hex literals after the wizard-gating CSS section.
  - Existing-test alignment with the new presentation contract: `tests/registrations/test_admin_review_flow.py` parametrize marker switched from `"priekšskatījums"` (retired link text) to `/admin/documents/` (preserved URL contract); `tests/registrations/test_admin_detail_ocr_rendering.py` two assertions updated to read the new `<dl class="fk-ocr-readout">` block plus translated confidence chip labels (now asserts `Vārds` present and `first_name` absent in the chip section).
  - New test file `tests/registrations/test_admin_inline_preview.py` (20 tests) covers: `_doc_preview_kind` parametric cases (`.jpg/.JPEG/.png/.webp/.heic/.pdf/.PDF/.docx/""/"no-ext"`); image-kind active doc renders `<img>` pointing at `documents:admin-document-preview`; pdf-kind renders `<iframe>` at the same URL; other-kind renders the fallback + `documents:admin-document-download` link with neither `<img>` nor `<iframe>` present; portrait panel renders no OCR `<dl>`; replaced docs only appear inside `<details class="fk-doc-history">` and never beside the active embed; `<dl class="fk-ocr-readout">` carries Latvian labels via `OCR_FIELD_LABELS`; `Iepriekšējās versijas` disclosure absent when no replaced docs; anonymous GET on the detail URL still redirects; anonymous GET on `documents:admin-document-preview` (the iframe target) still redirects.
  - Files NOT touched: `apps/registrations/services.py` (approval idempotency already correct), `apps/registrations/models.py` (no migration), `apps/documents/*` (preview endpoint and access control unchanged), the admin review queue template, every parent surface.
  - Full repo verification after the landing: `uv run pytest -q` → `878 passed`, `uv run ruff check .` → passed, `uv run mypy .` → passed (154 source files).
  - Manual LAN verification on 192.168.3.245 (2026-05-27) surfaced two corrections — both landed before merging Slice A to `main`:
    - **Revision A (2026-05-27): admin styling pivot + thumbnail/lightbox UX.** `templates/registrations/admin_review_detail.html` and `admin_review_queue.html` now `{% extends "admin/base_site.html" %}` — admin surfaces use Django admin's nav/shell/CSS, parent surfaces keep fk-* branding. All `fk-*` classes removed from admin templates; the Slice A block in `static/css/parent_theme.css` (`.fk-doc-panel*`, `.fk-doc-preview*`, `.fk-doc-history`, `.fk-doc-badge*`, `.fk-ocr-readout`, `.fk-ocr-confidence`, `.fk-review-actions`) was deleted. New `static/admin/css/review.css` defines `mms-review-*` classes scoped to admin. New `static/admin/js/doc_lightbox.js` (~75 LOC, vanilla, no deps) opens a single `<dialog class="mms-lightbox">` per page on thumbnail click; ESC, backdrop click, and a close button all dismiss; falls back to a `target="_blank"` link to the preview URL when JS or `HTMLDialogElement` is unavailable. `templates/registrations/admin/_doc_panel.html` rewritten as a `<ul class="mms-review-thumb-grid">` of ≈160 px thumbnails: image kinds render an `<img>` directly inside the anchor; PDF and other kinds render an inline feather-style SVG marked `data-doc-icon="<kind>"` plus filename caption. Each thumbnail anchor carries `data-mms-lightbox data-doc-kind="image|pdf|other"`; the lightbox JS clones the target into an `<img class="mms-lightbox-image">` for images or an `<iframe class="mms-lightbox-iframe">` for pdf/other. Replaced docs sit inside `<details class="mms-review-history">` with the per-doc `preview_kind` attached on the view side (`_build_doc_panel` sets `replaced_doc.preview_kind = _doc_preview_kind(replaced_doc)`), so replaced image docs open as `<img>` in the lightbox, not as iframe. Active vs replaced badges (`.mms-review-badge--active` / `--replaced`) and the OCR readout `<dl class="mms-review-ocr-readout">` + Latvian-labeled confidence chips `<ul class="mms-review-ocr-confidence">` carry over from Slice A's final shape.
    - **Slice A.1 (2026-05-27): templated review-action emails with application link.** `apps/registrations/services.py::_send_notification` replaced by `_render_and_send_notification(application, template_name, subject, extra_context=None)` which renders `templates/emails/registrations/<template_name>.txt` via `render_to_string`. Three new plain-text templates: `request_fix.txt` (links to `application_url`), `approve.txt` (links to `application_url`), `reject.txt` (links to `portal_url` — no edit affordance after rejection). Context includes `application`, `member_full_name`, `guardian_full_name`, `review_message`, `application_url` (= `settings.SITE_URL + reverse('registrations:application-workspace', args=[application.id])`), and `portal_url` (= `settings.SITE_URL + reverse('registrations:parent-portal')`). Plain-text only — no HTML alternative, no `EmailMultiAlternatives`. `apps/accounts/services.py` magic-link / verification emails are explicitly out of A.1 scope. New test file `tests/registrations/test_review_action_emails.py` (9 tests) covers: each action's subject + body + URL, recipient is the guardian, plain-text discipline (`getattr(msg, "alternatives", []) == []`), `SITE_URL` read at call time via `override_settings`, reject email contains the portal URL.
  - Full repo verification after Revision A + Slice A.1 landed: `uv run pytest -q` → `891 passed`, `uv run ruff check .` → passed, `uv run mypy .` → passed (155 source files). Manual LAN re-verification confirmed 2026-06-07 — admin shell renders as Django admin, thumbnails open the lightbox, replaced-image docs open as `<img>`, and the three review-action emails carry the application/portal URL.

- **P5 Slice B delivered — training-group assignment workflow (2026-05-28)**
  - Closes P5 acceptance item 3. Items 5, 7, 11 → Slice C; items 6, 8, 9, 10 → Slice D.
  - New service `apps/members/services.py::assign_training_group(member, group, actor)` — idempotent set/clear of `Member.training_group` (FK-id comparison + early return guarantees no UPDATE on a no-op, asserted via `CaptureQueriesContext`). Service layer is intentionally permissive (accepts inactive groups; picker filtering happens at the view). The `actor` parameter is plumbed but unused this slice (forward-compat with the P7 audit hook).
  - `apps/registrations/services.py::approve_application` extended with optional `training_group: TrainingGroup | None = None` kwarg. On first approval, passes it into `Member.objects.create(...)`. Inactive groups rejected with `ValueError("cannot assign inactive training group at approval time")` — the view maps this (plus `"can only approve submitted application"`) to Latvian copy before rendering the 400 page. Idempotency rule unchanged: re-approval ignores the kwarg; assignment edits go through `assign_training_group`.
  - `apps/registrations/views.py::admin_review_detail` gains `active_training_groups` (filtered `is_active=True`, ordered by `name`) and `current_inactive_group` (the assigned group on an approved member when that group is no longer active) context entries, plus a new `action == "assign_training_group"` POST branch guarded by `application.approved_member_id`. The existing `action == "approve"` branch now reads an optional `training_group` from POST and resolves the FK with a 400 fallback for an unknown id. ValueError from `approve_application` is mapped to Latvian admin copy ("Nevar piešķirt neaktīvu treniņu grupu apstiprināšanas brīdī." / "Var apstiprināt tikai iesniegtus pieteikumus.").
  - `templates/registrations/admin_review_detail.html` adds (a) a `<select name="training_group">` inside the existing approve form with a "— Piešķirsim vēlāk —" empty option and active groups only, and (b) a new post-approval `Treniņu grupa` module rendered when `application.approved_member` is set. Module shows `Pašreizējā grupa: <name>` (with `(neaktīva)` `<em>` marker when applicable) or `Vēl nav piešķirta.`; reassign picker carries `— Notīrīt piešķīrumu —` empty option, all active groups, and the currently-assigned inactive group (when present) with `selected data-inactive="true"` so existing state is never hidden.
  - `templates/emails/registrations/approve.txt` gains a conditional `Treniņu grupa: {{ application.approved_member.training_group.name }}.` line that renders only when a group was assigned at approval time. Standalone post-approval first-assignment and reassignment stay silent (no email) per the slice spec.
  - `static/admin/css/review.css` gains a small `/* P5 Slice B */` block: `.mms-review-group-module .form-row`, picker `min-width: 200px` shared between the approve-form pick and the post-approval module, and an `.mms-review-group-module em` rule for the inactive marker.
  - New tests: `tests/members/conftest.py` (shared fixtures: `guardian`, `member`, `training_group_a`, `training_group_b`, `inactive_training_group` — model imports deferred to fixture bodies to avoid `AppRegistryNotReady` at pytest collection); `tests/members/test_assign_training_group_service.py` (6 service tests including two `CaptureQueriesContext`-driven idempotency tests); `tests/registrations/test_admin_approval_with_group.py` (4 tests covering approve-without-group regression, approve-with-active-group, approve-with-inactive-group raises, and idempotent reapprove ignoring a different group); `tests/registrations/test_admin_review_group_assignment_ui.py` (10 end-to-end view+template tests covering dropdown active-only filter, approve+group, approve+empty, post-approval unassigned message, pre-selected option, reassign, clear, currently-assigned-inactive marker with `data-inactive="true"`, anonymous-blocked regression, and Latvian-error rendering on inactive-group POST). Existing `tests/registrations/test_review_action_emails.py` extended with two tests for the conditional email line.
  - No schema change, no migration, no new dependencies. Approval idempotency unchanged. Parent-facing surfaces unchanged.
  - Full repo verification: `uv run pytest -q` → `913 passed`, `uv run ruff check .` → passed, `uv run mypy .` → passed (160 source files).
  - Manual LAN verification on `http://192.168.3.245:8000/admin/review/applications/<id>/` confirmed 2026-06-07 — picker contents, approve+assign in one click, post-approval reassign/clear, inactive marker, and conditional email line all verified.

- **P5 Slice C delivered — Agreement model + generation + visibility (2026-05-28)**
  - Closes P5 acceptance items 5, 7, 11. Items 6, 8, 9, 10 → Slice D (DocuSeal-backed; instance provisioned, template config pending).
  - New `apps/agreements/` app with `Agreement` model: `ForeignKey(Member, on_delete=CASCADE, related_name="agreements")` + `is_current` boolean + partial `UniqueConstraint(fields=["member"], condition=Q(is_current=True), name="one_current_agreement_per_member")`. State machine `generated → sent → signed`, plus `void` (kept `is_current=True` until replaced) and a regenerate-after-void path. DocuSeal reservation fields (`external_provider`, `external_id`, `external_state`, `external_url`) reserved but unused this slice.
  - Services in `apps/agreements/services.py`: `get_current_agreement`, `create_agreement_for_member` (idempotent on non-void current; archives + creates fresh on void current; creates the first row when none exists), `mark_agreement_sent` / `mark_agreement_signed` (Latvian plain-text emails to the guardian; portal URL via `settings.SITE_URL + reverse('registrations:parent-portal')`), `void_agreement` (keeps `is_current=True`, no email, idempotent on void→void via `CaptureQueriesContext`-asserted early return), `regenerate_agreement` (refuses to clobber non-void with `ValueError("active agreement cannot be replaced")`; refuses None with `ValueError("no agreement exists to regenerate")`), `set_signing_path` (any state, no email, idempotent on same value).
  - `apps/registrations/services.py::approve_application` now wrapped in `@transaction.atomic` and creates the Agreement inside the same block, immediately after `Member.objects.create(...)`: `create_agreement_for_member(member, signing_path=application.preferred_agreement_signing or str(Agreement.SigningPath.ELECTRONIC))`. Idempotency rule unchanged.
  - `apps/registrations/views.py::admin_review_detail` gains `agreement` context entry (= `get_current_agreement(approved_member)`, or `None`) plus five new POST `action` branches: `mark_agreement_sent`, `mark_agreement_signed`, `set_signing_path`, `void_agreement`, `regenerate_agreement`. Each guards `agreement is None` (400 + "Līgums nav sagatavots."); `set_signing_path` additionally validates the path against `Agreement.SigningPath.choices`; `regenerate_agreement` maps only the expected "active agreement cannot be replaced" `ValueError` to the Latvian "Aktīvo līgumu nedrīkst aizvietot." copy and surfaces any unexpected message via `str(exc)`.
  - New partial `templates/registrations/admin/_agreement_module.html` renders state + signing-path picker + lifecycle timestamps + state-conditional action buttons (`Atzīmēt kā nosūtītu` for `generated`, `Atzīmēt kā parakstītu` for `generated|sent`, `Atcelt līgumu` `<details>` for any non-void, `Sagatavot jaunu līgumu` for `void`). Included from `admin_review_detail.html` after the Slice B Treniņu grupa module, gated by `{% if agreement %}`.
  - `apps/agreements/presentation.py::agreement_status_copy(agreement)` returns the Latvian status line per `(state, signing_path)`: generated → "Līgums sagatavots, drīzumā saņemsiet to parakstīšanai."; sent+electronic → "Līgums nosūtīts uz e-pastu parakstīšanai."; sent+paper → "Klubs sazināsies ar Jums par līguma parakstīšanu."; signed → "Līgums parakstīts ✓"; void → "Līgums atcelts.".
  - `templates/registrations/parent_portal.html` renders the copy per approved-application card (the view annotates `app.agreement_status` on each ORM instance inside the existing iteration, matching the pre-existing `app.can_edit` annotation pattern). `templates/registrations/application_workspace.html` renders the same copy in the read-only post-approval branch near the application-status banner. Pre-approval surfaces show no agreement line — verified by an `fk-app-agreement-status` class-absence assertion.
  - Two new plain-text email templates under `templates/emails/agreements/`: `sent.txt` (path-aware copy: electronic / paper branches) and `signed.txt`. Both link to the portal via `settings.SITE_URL`. The Slice D refinement that will suppress Django's email for the electronic path once DocuSeal handles native notifications is explicitly out of slice; for Slice C and for the paper path forever, Django sends.
  - Read-only Django admin: `AgreementAdmin` overrides `has_add_permission`, `has_change_permission`, and `has_delete_permission` to return False — the change view is genuinely closed (no Save button), only the list and read-only detail are reachable.
  - New tests: `tests/agreements/conftest.py`, `tests/agreements/test_agreement_model.py` (6 tests: defaults + unique-current constraint + choices + cascade), `tests/agreements/test_agreement_services.py` (~23 tests: full state machine, regenerate-after-void, idempotency proofs via `CaptureQueriesContext`, email body + `SITE_URL` via `override_settings`, subject-line + recipient assertions), `tests/registrations/test_admin_review_agreement_ui.py` (11 tests: 5 POST branches + visibility on submitted vs approved), `tests/registrations/test_agreement_visibility_parent_surfaces.py` (7 tests: portal + workspace copy per (state, signing_path) pair). Existing `tests/registrations/test_admin_approval_with_group.py` extended with 3 agreement-integration tests on `approve_application`.
  - One migration: `apps/agreements/migrations/0001_initial.py`.
  - Full repo verification: `uv run pytest -q` → `963 passed`, `uv run ruff check .` → passed, `uv run mypy .` → passed (174 source files).
  - Manual LAN verification on `http://192.168.3.245:8000/admin/review/applications/<id>/` confirmed 2026-05-29 — full Slice C functionality + the five LAN-feedback polish patches below.
  - LAN-feedback polish (2026-05-28 / 2026-05-29):
    - `Agreement.get_absolute_url()` returns the source-application review-detail URL → Django admin's auto-rendered "VIEW ON SITE" button now bridges from the read-only Agreement detail to the surface where transitions actually live.
    - `RegistrationApplicationAdmin.save_model` calls `sync_application_signing_path_to_agreement` so staff edits to `preferred_agreement_signing` propagate to the active agreement.
    - `void_agreement` now sends a Latvian plain-text notification (`Jūsu līgums ir atcelts`, subject + body include the staff-supplied reason when present + portal URL). Void→void stays idempotent — no second email, no second UPDATE (both asserted via `CaptureQueriesContext` + outbox count). This reverses the earlier "no email on void" decision per LAN feedback that parents need to know when an agreement is cancelled.
    - Migration `apps/agreements/migrations/0002_backfill_agreements_for_approved_members.py` creates a `generated`-state Agreement for every approved Member that lacked one pre-Slice-C. Signing path taken from the source application's `preferred_agreement_signing` (default `electronic`); `generated_at` from the application's `reviewed_at` (else `now()`). Idempotent per-member existence guard. Reverse migration drops only fresh-looking backfill rows.
    - Bidirectional signing-path sync invariant: `application.preferred_agreement_signing == agreement.signing_path` post-approval, always. `set_signing_path` (Līgums picker) writes both fields; `sync_application_signing_path_to_agreement` updates the agreement at any state (the previous `generated`-only gate is removed). Single source-of-truth-at-the-data-level dissolves the drift the LAN tester surfaced.

- **P5 Slice D delivered — DocuSeal adapter + signed-state sync (2026-06-06)**
  - Closes P5 acceptance items 6, 8, 9, 10. Completes P5.
  - New `apps/integrations/agreement_platform.py` boundary mirrors the OCR adapter: exception taxonomy (`AgreementPlatformError` + `ConfigError`/`AuthError`/`NotFoundError`/`TransientError`), frozen `SubmissionResult(external_id, external_url, external_state)`, and `create_submission` / `sync_submission` / `archive_submission` / `verify_webhook_signature` dispatching on `settings.AGREEMENT_PROVIDER_MODE`. `stub` (default) returns deterministic `stub-{id}` values and a True signature; `docuseal` lazy-imports the concrete provider; an unknown mode raises `ConfigError`.
  - `apps/integrations/docuseal.py` is the real provider: HTTP via `requests.request` with an `X-Auth-Token` header, `_require_config()` (api_url / api_key / int template_id), submitter + field-payload builders (child + guardian fields; **no** training group), status normalization, and a `_request` status→exception mapping (401/403→Auth, 404→NotFound, ≥500→Transient, ≥400→Config, Timeout/RequestException→Transient). `verify_webhook_signature` matches DocuSeal's scheme: the `X-Docuseal-Signature` header is `<timestamp>.<hex-hmac>` with the HMAC-SHA256 computed over `<timestamp>.<raw-body>`, enforces a 300s freshness window, and uses `hmac.compare_digest`.
  - `apps/integrations/tasks.py` gains django-q2 jobs `create_agreement_submission` / `sync_agreement_submission` / `archive_agreement_submission` (+ `enqueue_*` helpers). Transient classified failures raise `RetryableAgreementError` so the cluster retries; terminal failures persist `external_state="failed"` + `external_error_code` and return. `sync` drives `mark_agreement_signed` only when the agreement is still `generated`/`sent` (the bidirectional signing-path invariant).
  - `Agreement.external_error_code` (CharField, blank default) added (migration `0003`). `apps/agreements/messages.py` maps stored codes to Latvian copy (`get_agreement_error_message`, generic fallback).
  - Email suppression: `apps/agreements/services.py::_should_send_email` suppresses the `sent`/`signed` guardian emails on the **electronic** path (DocuSeal notifies the signer); `void` always emails; paper sends on every transition. `mark_agreement_sent` optimistically flips to `sent`, suppresses the email, and enqueues the DocuSeal create job — but if an electronic agreement has no guardian email it degrades to paper first (no enqueue). `void_agreement` enqueues an archive job for an electronic agreement with a live `external_id`. Enqueue helpers are imported lazily inside the functions to avoid the `tasks → services` circular import.
  - Webhook: `apps/agreements/webhooks.py::docuseal_webhook` (`@csrf_exempt @require_POST`) verifies the HMAC, then drives a matching `sent`/`generated` agreement to `signed`. Bad signature → 403; unknown id / wrong event / malformed body / already-signed → 200 (so DocuSeal does not retry). Mounted via `apps/agreements/urls.py` at `integrations/docuseal/webhook/`, included in `fk_cesis_mms/urls.py` **before** the registrations catch-all.
  - `admin_review_detail` Līgums module: a failed electronic submission surfaces the Latvian error + a "Mēģināt vēlreiz" button (`retry_docuseal` → re-enqueue create); a live submission shows an "Atvērt DocuSeal" link + a "Pārbaudīt DocuSeal statusu" button (`sync_docuseal` → enqueue sync). Two new POST branches wire the buttons; `agreement_error_message` added to the view context.
  - Repo testing convention reaffirmed: HTTP and enqueue spies use `unittest.mock.patch` / `monkeypatch` (the repo does **not** use `pytest-mock`/`mocker` or the `responses` library). Conftest fixtures are package-scoped, so `tests/integrations/conftest.py` re-declares the agreement guardian/member fixtures.
  - New tests (~40): `tests/integrations/test_agreement_platform_adapter.py`, `test_docuseal_provider.py`, `tests/agreements/test_agreement_messages.py`, `test_agreement_tasks.py`, `test_electronic_email_suppression.py`, `test_electronic_flow_integration.py`, `test_docuseal_webhook.py`, `tests/registrations/test_agreement_module_docuseal.py`. Pre-existing service tests that assert sent/signed email delivery moved to the paper path.
  - One migration: `apps/agreements/migrations/0003_agreement_external_error_code.py`.
  - Plan: `docs/superpowers/plans/2026-05-29-p5-slice-d-docuseal-adapter.md`. Design: `docs/superpowers/specs/2026-05-29-p5-slice-d-docuseal-adapter-design.md`.
  - **Live DocuSeal validation (2026-06-07)** against a real self-hosted instance surfaced bugs the stub fixtures hid (mirrors the P3 live-validation lesson). All fixed and re-verified live:
    - Webhook signature was verified in the wrong format — DocuSeal sends `<timestamp>.<hex-hmac>` over `<timestamp>.<raw-body>`, not a bare HMAC of the body; every real webhook would have 403'd (`e1ff2dc`).
    - `create_submission` parsed the response as an object, but DocuSeal returns a JSON **array** of submitter objects; the submission id is `submission_id` and the signing URL is `embed_src` (`30154be`).
    - Prefill must omit the submitter `role` — once field data is present DocuSeal strictly validates the role against the template ("First Party") and 422s a mismatch (`70abb42`).
    - Guardian `email` + `phone` template fields were missing from the payload, so they rendered blank (`d74d6cd`).
    - `agreement_date` is no longer sent — the template auto-fills the current date (`75a41ae`).
    - Prefill now goes through the submitter `fields` array as `readonly:true` entries (not a `values` map): readonly fields are non-interactive, so the signer skips field-by-field re-confirmation and lands straight on the signatures. Confirmed in the signing UI (`170f988`).
  - Manual LAN smoke (stub mode) confirmed 2026-06-07 for the non-DocuSeal transitions — void email + archive enqueue, empty-email → paper fallback, and paper-path emails all verified. **P5 fully LAN-verified and signed off 2026-06-07.**

- **P6 guardian-identity Slice A delivered — canonical Guardian + dedup (2026-06-10)**
  - `Guardian` is now 1:1 with `ParentAccount` via `Guardian.parent_account` (OneToOneField, `on_delete=PROTECT`, nullable). Migration `members/0004`.
  - `RegistrationApplication.guardian` FK added (`on_delete=PROTECT`, nullable, `related_name="applications"`). Migration `registrations/0009`.
  - New `apps/members/services.py::resolve_guardian_for_account(account)` — `get_or_create` keyed by `parent_account`, mirrors `email` on create.
  - `create_or_update_draft` attaches `application.guardian = resolve_guardian_for_account(verified_account)` at initiation (and on every verified save).
  - `approve_application` now REUSES `application.guardian` (with a `resolve_guardian_for_account` fallback for account-bearing apps; bare create only for account-less ORM-built apps), refreshes the Guardian profile from the application snapshot, and persists the link via `update_fields`. The previous unconditional `Guardian.objects.create(...)` is gone.
  - Effect: a parent's children share ONE `Guardian`, so the sibling discount applies correctly and there is one Invoice Ninja client per parent.
  - Scope note: the denormalized `guardian_*` columns on `RegistrationApplication` remain in place — read-through + column drop is Slice B; locked-profile UX + admin-initiated email change is Slice C; parent self-service email change is deferred.
  - New tests: `tests/members/test_guardian_resolution.py` (3 tests: 1:1 link, unique constraint, `resolve_guardian_for_account` idempotency), `tests/registrations/test_guardian_dedup.py` (4 tests including the sibling-discount payoff across two approved children; 7 tests total across both files).
  - Full-repo gate: `uv run pytest -q` → **1133 passed**, `uv run ruff check .` → clean, `uv run mypy .` → clean (245 source files).
  - **LAN acceptance verified (2026-06-10)** on a fresh-start instance (`uv` runserver + qcluster on `0.0.0.0:8000`, SQLite, stub OCR, live Invoice Ninja). Plan: `docs/acceptance/2026-06-10-p6-guardian-identity-slice-a-lan-acceptance.md`. Scenarios A (child #1 flow), B (child #2 same parent, opt-out enforced), C (**one Guardian / two Members per parent**), D (idempotent re-approval), E (**sibling discount 300 / 150**), H (abandoned draft benign) all PASS. G (profile latest-wins) correct but not differentiated (both apps carried identical guardian data).
  - **Concurrency bug found + fixed during scenario F** (commits `2fa6510`, `7470d2f`): the admin push enqueues one `push_billing_record` per record; sibling records run on parallel django-q workers and each did an unlocked check-then-create on the shared `guardian.external_client_id` / `plan.external_product_id`, producing **duplicate Invoice Ninja clients and products** (invoices split across two clients). Slice A newly exposes the *client* race (siblings now share one guardian); the *product* race was latent P6 debt. Fix: `_ensure_client_id` / `_ensure_product_id` helpers in `apps/integrations/tasks.py` re-read the row under `select_for_update()` inside `transaction.atomic()` so a concurrent worker reuses the committed id. After the fix, scenario F re-test PASS: **one IN client + one product, both children's invoices under the single client** (`MMS-1-1` €300 + `MMS-2-1` €150). Known limitation: `select_for_update` is a no-op on SQLite (so the live re-test validated the reuse path sequentially); the lock guarantee holds on the Postgres deploy target and is covered by unit tests in `tests/billing/test_push_billing_record.py`. Per-record resources (installment rows, invoice numbers) are unaffected (record-scoped). A secondary latency note: the outbound IN call runs while the row lock is held — acceptable at current scale, revisit under load.
  - Gate after the fix: `uv run pytest -q` → **1137 passed**, ruff + mypy clean.

- **P6 guardian-identity Slice B1 delivered — guardian read-through (2026-06-10)**
  - 5 guardian-read accessors added on `RegistrationApplication`: `guardian_name`, `guardian_pid`, `guardian_contact_phone`, `guardian_address`, `guardian_contact_email`. The 4 profile accessors (`guardian_name`, `guardian_pid`, `guardian_contact_phone`, `guardian_address`) treat a linked `Guardian` as the source of truth and fall back to the denormalized column only when no Guardian is linked. The email accessor prefers `parent_account.email`, else falls back to the `guardian_contact_email` column.
  - Guardian profile is now populated at draft-save (`create_or_update_draft`), not only at approval. This means every save to an in-progress draft refreshes the Guardian's name/PID/phone/address from the submitted form fields.
  - Templates (admin review detail, admin review queue, parent portal, workspace email), the workspace form `initial` dict, `get_application_prefill`, the `submit_application` phone-sync, and `_render_and_send_notification` all read guardian data via the accessors. `approve_application` no longer copies the snapshot onto the Guardian (the Guardian profile is already kept current by draft-save).
  - Net effect: editing a guardian's profile propagates to every application and every agreement that renders it. Agreements already read `member.guardian.*`, so they propagate for free — no template changes needed there.
  - Scope note: the `guardian_*` columns remain on `RegistrationApplication` (dual-written for safety). Slice B2 drops those columns, simplifies the accessors (removes the column fallback), and sweeps the `create(guardian_*=…)` test fixtures. Slice C is the locked-profile UX + admin email-change workflow.
  - New test file `tests/registrations/test_guardian_read_through.py` (9 tests). Full-repo gate: `uv run pytest -q` → **1146 passed**, `uv run ruff check .` → clean, `uv run mypy .` → clean (246 source files).
  - Plan: `docs/superpowers/plans/2026-06-10-p6-guardian-identity-slice-b1-read-through.md`.

- **P6 guardian-identity Slice B2 delivered — drop guardian columns (2026-06-11)**
  - The five `guardian_*` columns (`guardian_full_name`, `guardian_personal_id`, `guardian_contact_phone`, `guardian_declared_address`, `guardian_contact_email`) are DROPPED from `RegistrationApplication`. Migration `registrations/0010`.
  - The read accessors (`guardian_name`, `guardian_pid`, `guardian_contact_phone`, `guardian_address`, `guardian_contact_email`) now read the `Guardian` / `ParentAccount` only — no column fallback. The email accessor prefers `parent_account.email`; the four profile accessors use the linked `Guardian` exclusively (empty string when no Guardian is linked).
  - `create_or_update_draft` writes only the `Guardian` row. Unverified drafts hold only `claimed_email` — the Guardian profile requires a verified account.
  - Submit validation reads guardian fields via `_GUARDIAN_SUBMIT_ACCESSORS` (the five accessor names).
  - `RegistrationApplication.__str__` uses `guardian_contact_email` (falls through to `claimed_email` when no account is linked).
  - Django admin `search_fields` use `parent_account__email` and `guardian__full_name` (no longer the dropped columns).
  - `make_guardian` test fixture added in `tests/conftest.py` (or `tests/registrations/conftest.py`) to create a linked `Guardian` in one call. ~24 test files swept off the dropped columns (all `RegistrationApplication.objects.create(guardian_*=…)` replaced with `make_guardian` or accessor-based assertions).
  - `parent_portal` and `admin_review_queue` querysets use `select_related("guardian", "parent_account")` to avoid N+1 from the email accessor traversing `parent_account` (Slice B2 carryover).
  - Carryover test: `test_update_existing_draft_repopulates_guardian_profile` in `tests/registrations/test_guardian_read_through.py` (total 12 tests in file).
  - Full-repo gate: `uv run pytest -q` → **1149 passed**, `uv run ruff check .` → clean, `uv run mypy .` → clean (247 source files).
  - **LAN acceptance COMPLETE — signed off 2026-06-11** (`docs/acceptance/2026-06-11-p6-guardian-identity-slice-b1-b2-lan-acceptance.md`). Browser checks (local): propagation across a parent's apps (edit guardian on one → visible on siblings + portal + admin), admin search by `parent_account__email` / `guardian__full_name`, no display regression — all pass. Deploy checks on `:dev` (Postgres): migration `0010` applied, services healthy (qcluster has no healthcheck by design — no HTTP server). **Migration `0010` is lossy** (drops columns, no data migration): 2 `guardian__isnull=True` rows on `:dev` were disposable old test apps. **Prod cutover must be fresh-start** (no pre-guardian-identity rows) to avoid guardian-data loss on real applications.
  - Guardian-identity work now has only **Slice C** remaining: locked-profile UX + admin-initiated email change workflow.

- **P6 guardian-identity Slice C delivered — locked profile UX + admin email change (2026-06-11)**
  - **Code-only — no migrations.** Closes the last two design-spec acceptance items (#4 locked profile + propagation, #6 admin email change). Plan: `docs/superpowers/plans/2026-06-11-p6-guardian-identity-slice-c.md`.
  - **Lock signal:** new read-through property `RegistrationApplication.guardian_profile_populated` (`True` when a `Guardian` is linked and `guardian.full_name` is set). The view locks when `editable and guardian_profile_populated`.
  - **Form locking (`RegistrationApplicationForm`):** the verified email field (`guardian_email`) is **always** `readonly` (it is the OTP identity — staff-only change). When `guardian_profile_locked=True`, the four profile fields (`guardian_full_name`, `guardian_personal_id`, `guardian_phone`, `guardian_declared_address`) get `readonly`. Deliberately `readonly`, NOT `disabled` — `disabled` inputs drop from POST and `create_or_update_draft` would wipe the `Guardian`; `readonly` round-trips the values.
  - **View wiring:** `application_workspace` computes the flag once and passes it to the GET form, the POST-rerender form, and the template context; `submit_registration` does the same for its invalid-submit re-render. `new_registration` (no-JS fallback, no toggle) stays unlocked.
  - **Template:** `application_workspace.html` renders a Latvian unlock toggle ("Rediģēt vecāka datus") + scoped inline JS (mirrors the `SameAddressSync` pattern) that strips `readonly` from the four profile inputs on click; the email stays `readonly`. Gated on `section_name == "guardian" and guardian_profile_locked`. Parent-surface copy-contract test passes (no English leak).
  - **Admin email change:** new `apps.accounts.services.change_parent_email(account, new_email)` — atomic, normalizes (`strip().lower()`), no-op when unchanged, enforces uniqueness (raises `ValueError` if another account owns the email, case-insensitive), and syncs the `Guardian.email` mirror (IN client contact reads it). Safe when no `Guardian` is linked. A DB-level collision (TOCTOU race past the pre-check) is caught via a nested savepoint and converted to `ValueError`, so the error contract is consistent.
  - **Admin:** new `apps/accounts/admin.py` registers `ParentAccount` (was unregistered). `ParentAccountAdmin.save_model` routes an email change through the service (single writer) via a temporary-revert pattern (reset `obj.email` to `form.initial`, save other fields, then `change_parent_email`). The admin ModelForm's `unique=True` validation rejects dup emails before `save_model`, so the service `ValueError` is the programmatic guard, not the normal admin path.
  - Propagation is automatic via read-through: an unlock-and-edit writes the one `Guardian` row, so every application + agreement display reflects it; an admin email change surfaces on `application.guardian_contact_email` (= `parent_account.email`).
  - Full-repo gate: `uv run pytest -q` → **1169 passed**, `uv run ruff check .` → clean, `uv run mypy .` → clean (251 source files). (`ruff format` is not an enforced gate in this repo.)
  - Known minor UX polish deferred (non-blocking): on a rare non-AJAX submit validation error, an unlocked-then-edited profile re-locks (one click to re-unlock); no focus move to the first field after unlock. **Parent self-service email change stays deferred** (needs OTP re-verification of the new address — see milestone gaps).
  - **LAN acceptance: COMPLETE — signed off 2026-06-11** (`docs/acceptance/2026-06-11-p6-guardian-identity-slice-c-lan-acceptance.md`, build `dev` @ `1dd1a93`, local `uv` instance + Playwright). L1 lock+unlock (four profile fields readonly→editable on toggle; email stays readonly), L2 propagation (edit on app 49 → visible on sibling app 50 via shared Guardian FK, and on the `/portal/` application card title; an earlier "portal doesn't surface the guardian name" note was an `innerText` measurement artifact — the name is rendered + visible), L3 first-registration unlocked (no toggle, fields editable, email readonly), L4 admin email change (ParentAccount + Guardian mirror + read-through accessor all updated; collision with another account's email rejected by admin unique validation with no 500) — **all PASS**.

- **Portal personalized greeting (2026-06-12)** — the `/portal/` hero card greets the verified parent by their canonical `Guardian` name: `Sveiks/-a, {full_name}!` (the previously static `Sveiks!`). `parent_portal` passes `greeting_name` (account's `Guardian.full_name`, or `""`) to the context; `templates/parent_ui/includes/hero_card.html` renders the personalized form when a name is on file, else falls back to plain `Sveiks!` (fresh parent without a name yet). Tests: `tests/registrations/test_portal_greeting.py`. Verified live (greeting renders "Sveiks/-a, Jānis Bērziņš!").

- **P6 invoice issue/send policy delivered — scheduled per-installment send (2026-06-12)**
  - **Code-only schema add (`BillingInvoice.sent_at`, migration `billing/0008`) + one schedule migration (`billing/0009`).** Spec: `docs/superpowers/specs/2026-06-12-p6-invoice-issue-send-policy-design.md`; plan: `docs/superpowers/plans/2026-06-12-p6-invoice-issue-send-policy.md`.
  - **Problem solved:** the IN push creates invoices as **Draft** (invisible to the guardian, no email). With installments materialized across future months, "auto-issue+email on push" would email a parent their whole season at once. So *create* (push, unchanged) is separated from *issue+send* (a nightly sweep, per installment).
  - **Send rule:** `apps.billing.services.is_invoice_due_to_send(invoice, today)` — eligible when the invoice has an `external_invoice_id`, is still Draft (`external_status == "created"`), and `today >= due_date.replace(day=1)` (on/after the 1st of its due month; also catches mid-season signups + overdue catch-up). One email per installment, ~19 days before the default 20th due day.
  - **Mechanism:** `invoice_platform.email_invoice(external_invoice_id)` → `invoice_ninja.email_invoice` issues `POST /api/v1/invoices/bulk {"action":"email","ids":[id]}`; emailing a Draft in IN flips it to Sent and sends IN's templated invoice email (PDF + payment link — content configured in Invoice Ninja, not this app).
  - **The job:** `apps.integrations.tasks.send_due_invoices()` — nightly sweep, gated by `BILLING_AUTOSEND_ENABLED`. Selects Draft invoices, skips not-yet-due, skips+logs guardians with no email, emails the rest, and on success sets `external_status="sent"` + `sent_at`. Per-row isolation (one bad row never aborts); a failed send records `external_error_code` and **stays `"created"`** (never `"failed"`) so the next nightly run retries — the cadence is the retry loop. Registered as the DAILY `django-q` Schedule `billing-send-due-invoices` (`billing/0009`, mirrors the payment-sync `0005`), runs in the existing `qcluster`.
  - **Safety switch:** `BILLING_AUTOSEND_ENABLED` (env, **default false**) — deploy the machinery, verify on one parent, then set `BILLING_AUTOSEND_ENABLED=true` in prod to activate. `BILLING_SEND_DUE_HOUR` (default 4) sets the sweep hour. Both in `.env.example`.
  - **Out of scope:** IN recurring invoices/auto-billing; payment reminders/dunning; in-app "send now" action (staff use Invoice Ninja's UI for ad-hoc sends); changing the create-at-push behavior.
  - Full-repo gate: `uv run pytest -q` → **1195 passed**, `uv run ruff check .` → clean, `uv run mypy .` → clean (260 source files).
  - **LAN acceptance: COMPLETE — signed off 2026-06-12** (`docs/acceptance/2026-06-12-p6-invoice-issue-send-policy-lan-acceptance.md`, build `dev` @ `f531706`, real IN `in.mplytics.eu`, flag toggled per step, IN `status_id` verified via API). S1 push→3 Draft no email; S2 flag-off no-op; S3 due installment→Sent (`status_id=2`) + **parent invoice email received** (FK Cēsis branding, €100, Latvian), futures stay Draft; S4 idempotent (no re-send); S5 no-email guardian skipped — **all PASS**. Test IN data deleted + local DB restored afterward.
  - **Operational note before prod activation:** IN also emits an admin "Invoice Sent" notification to the IN account owner per send (an Invoice Ninja per-user notification preference, NOT from this app). Disable it in IN (Settings → User Details → Notifications → "Invoice Sent") so staff aren't emailed once per installment during nightly bulk sends. Then activate with `BILLING_AUTOSEND_ENABLED=true`.

- **P7 Slice C-i delivered — admin review consolidation (2026-06-14)**
  - The registration staff-review flow now lives entirely in the **Django admin**. The bespoke custom views `admin_review_queue` + `admin_review_detail` (and `_require_staff`), their two `admin/review/...` URL routes, and the `admin_review_queue.html` / `admin_review_detail.html` templates are **deleted**. Spec: `docs/superpowers/specs/2026-06-14-p7-admin-review-consolidation-design.md`; plan: `docs/superpowers/plans/2026-06-14-p7-admin-review-consolidation.md`.
  - **`RegistrationApplicationAdmin` change page** = review + edit on one screen: a custom `change_form_template` renders the document/OCR panels (+ lightbox), the agreement module, and the training-group module **above** the native edit form, with the flow actions in a **top action bar** (no more bottom-scroll). Panel context is injected by a `change_view` override calling `apps/registrations/admin_panels.py::build_review_context` (the review-context builder extracted from the old view).
  - **Per-object action endpoints** via `get_urls()`: `review-action/<id>/` (POST dispatch ported verbatim for reject/request-fix/assign-group + all agreement transitions — services unchanged + still audited; service `ValueError`→admin message, not 500) and `approve/<id>/` (GET confirm page + POST commit). Reject/approve → changelist; others → change page. Endpoints are `has_change_permission`-gated + CSRF-safe.
  - **Changelist** is the entry point (post-action redirect target): a status-aware `quick_actions` column renders a **true one-click POST** for the safe agreement next-step (generated→"Atzīmēt nosūtītu", sent→"Atzīmēt parakstītu", CSRF token minted from the request) + an "Atvērt →" open link. The old `review_link` (which bounced to the deleted custom page) is gone.
  - The reused partials `_doc_panel.html` / `_agreement_module.html`, `review.css`, `doc_lightbox.js` are kept (now loaded on the admin change page via `Media`); the agreement-module forms target the new endpoint. `Agreement.get_absolute_url` (model) repointed to the admin change page.
  - Full-repo gate: `uv run pytest -q` → **1242 passed**, ruff + mypy clean (288 source files), `makemigrations --check` clean. No model changes / no migrations. **C-ii remains** (cross-links application↔member↔agreement↔billing, sync-health badges/filters, search/filter polish, training-group de-duplication).

- **P7 Slice C-ii batch 1 delivered — admin quick wins (2026-06-14)**
  - First batch of three user-prioritised admin flow-polish items. Spec: `docs/superpowers/specs/2026-06-14-p7-cii-admin-quick-wins-design.md`; plan: `docs/superpowers/plans/2026-06-14-p7-cii-admin-quick-wins.md`.
  - **Registrations app first in the admin menu:** a thin `apps/core/admin_site.py::FkAdminSite` overrides `get_app_list` (stable sort, `registrations` first; all other apps keep Django's default order), wired via `apps/core/apps.py::FkAdminConfig(AdminConfig)` with `default_site` and the `"django.contrib.admin"` → `"apps.core.apps.FkAdminConfig"` swap in `INSTALLED_APPS`. `FkAdminConfig` sets `default = False` (and imports the admin apps *module*, not the `AdminConfig` class) so it does not collide with `CoreConfig` during the `apps.core` auto-discovery scan.
  - **Agreement-status column** on the applications changelist: `RegistrationApplicationAdmin.agreement_status` (`@admin.display`, "Līguma statuss") renders `get_current_agreement(...).get_state_display()` (concise state label, not the verbose parent copy) or "—".
  - **One-click billing confirm:** `BillingRecordAdmin` gains a `get_urls()` POST endpoint `billing_billingrecord_confirm` (`admin_view`-wrapped, `has_change_permission`-gated, DRAFT→CONFIRMED via `save(update_fields=["status","updated_at"])`, no-op+info message when already confirmed, `_safe_redirect` honoring a validated `next` with `require_https=request.is_secure()`), a `change_form_template` top "Apstiprināt ierakstu" button (DRAFT only), and a per-row `confirm_action` one-click POST form on the changelist (CSRF token via `get_token(self._request)`). Confirm is intentionally **not** audited this batch (deferred — would need an `AuditEvent` choices value + migration).
  - Full-repo gate: `uv run pytest -q` → **1252 passed**, ruff + mypy clean (292 source files), `makemigrations --check` clean. No model changes / no migrations. **Broader C-ii remains** (cross-links application↔member↔agreement↔billing, sync-health badges/filters, search/filter polish, document active-vs-replaced UX, training-group de-duplication).

- **P7 Slice C-ii batch 2 — Plan 1 (cross-links) delivered (2026-06-15)**
  - The Guardian ↔ Application ↔ Member ↔ Agreement ↔ Billing web is now navigable both ways from the admin. Spec: `docs/superpowers/specs/2026-06-15-p7-cii-batch2-admin-polish-design.md`; plan: `docs/superpowers/plans/2026-06-15-p7-cii-batch2-plan1-cross-links.md`.
  - **Shared helper** `apps/core/admin_links.py`: `admin_link(obj, label=None)` (anchor to the object's admin change page via `obj._meta`; `"—"` for None; escaped plain-text fallback on `NoReverseMatch`) + `admin_links(objs, *, limit=10)` (capped to-many list with a "+N" overflow marker).
  - **Related-records rows** ("Saistītie ieraksti") on Member/Guardian (`apps/members/admin.py`), Agreement (`apps/agreements/admin.py`), and BillingRecord (`apps/billing/admin.py`) change pages (readonly display methods); the registrations review page renders the same block via a `related_links` dict added to `build_review_context`.
  - **Changelist link columns:** applications gain a clickable **Biedrs** column (`member_link`); billing's plain `guardian_name` becomes a clickable **Vecāks** link (`guardian_link`) + a new **Līgums** column (`agreement_link`). `BillingRecordAdmin.get_queryset` adds `select_related("member", "member__guardian", "agreement")` to avoid an N+1.
  - Full-repo gate: `uv run pytest -q` → **1267 passed**, ruff + mypy clean (298 source files), `makemigrations --check` clean. No model changes / no migrations. **Remaining C-ii:** Plan 2 (sync-health badges/filters + search/filter polish + document active-vs-replaced) and Plan 3 (training-group de-duplication).

- **P7 Slice C-ii batch 2 — Plan 2 (visibility) delivered (2026-06-15)**
  - Admin records are now scannable: sync-health badges, search/filter polish, document active-vs-replaced. Plan: `docs/superpowers/plans/2026-06-15-p7-cii-batch2-plan2-visibility.md`.
  - **Shared helper** `apps/core/admin_badges.py::status_badge(text, level, *, tooltip="")` (CSS-classed span, levels ok/fail/pending/muted, escaped text+tooltip) + `static/admin/fk_badges.css` (attached via each admin's `Media`).
  - **Sync-health:** `BillingRecordAdmin` `external_status`/`payment_status` columns are now color badges (Latvian error tooltip via `get_invoice_error_message`) with a `SyncHealthFilter` (OK / Neizdevās / Procesā / Nav sinhronizēts, non-overlapping buckets on `external_status`+`external_error_code`); `AgreementAdmin` gains a `sync_health_badge` column (tooltip via `get_agreement_error_message`) + `AgreementSyncHealthFilter` (Agreement uses `external_state`, and `external_error_code` is now surfaced on the detail page).
  - **Search/filter/date polish:** `DocumentAdmin` gains `search_fields`/`list_filter`/`date_hierarchy(uploaded_by_parent_at)`/ordering; `TrainingGroupAdmin` + `MembershipPlanAdmin` get `search_fields`; `RegistrationApplicationAdmin` gains `date_hierarchy(submitted_at)`/ordering + a `preferred_agreement_signing` filter; billing/agreement admins gain `date_hierarchy`/ordering.
  - **Document active-vs-replaced:** `DocumentAdmin` shows an **Aktīvs/Vēsturisks** badge column + a `DocumentStateFilter` (active/replaced on `deleted_at`), default `-uploaded_by_parent_at` order — fixing the flat list that showed soft-deleted docs identically to active ones.
  - Full-repo gate: `uv run pytest -q` → **1292 passed**, ruff + mypy clean (304 source files), `makemigrations --check` clean. No model changes / no migrations. **Remaining C-ii:** Plan 3 (training-group de-duplication, one migration).

- **P7 Slice C-ii batch 2 — Plan 3 (training-group de-duplication) delivered — C-ii COMPLETE (2026-06-15)**
  - Plan: `docs/superpowers/plans/2026-06-15-p7-cii-batch2-plan3-group-dedup.md`.
  - **Case-insensitive unique name:** `TrainingGroup.Meta.constraints` gains `UniqueConstraint(Lower("name"), name="uniq_training_group_name_ci")` (migration `members/0005` — the **only** migration in batch 2) + a model `clean()` raising a Latvian `ValidationError` on a case-insensitive clash (so the admin form shows a friendly error, not just a DB `IntegrityError`).
  - **Merge action:** `TrainingGroupAdmin.merge_training_groups` — select ≥2 groups → a confirmation page (`templates/admin/members/traininggroup/merge_confirm.html`) to pick the target → inside `transaction.atomic()`, reparent all members of the others to the target (`Member.objects.filter(training_group__in=others).update(...)`) then bulk-delete the spares. Gated on `has_delete_permission`; the target is validated to be one of the selected groups (no arbitrary-target reparent); single-group selection is a no-op warning. Merge is not audited this batch (deferred). `search_fields=("name",)` added.
  - Full-repo gate: `uv run pytest -q` → **1301 passed**, ruff + mypy clean (307 source files), `makemigrations --check` clean (migration `0005` committed). **P7 Slice C (admin ops/export/audit polish) is now fully delivered** — C-i + C-ii batch 1 + C-ii batch 2 (Plans 1–3).

- **Guardian/ParentAccount consolidation — Plan 1 (de-dup fields + 1:1) delivered (2026-06-15)**
  - Removes the duplicated contact data: `Guardian.email`/`phone` are no longer columns — they are read-only **`@property` proxies** of the linked `ParentAccount` (the single source of truth). `Guardian.parent_account` is now **NOT NULL** (still 1:1). Spec: `docs/superpowers/specs/2026-06-15-guardian-parentaccount-consolidation-design.md`; plan: `docs/superpowers/plans/2026-06-15-guardian-consolidation-plan1-model-migration.md`.
  - **Data migration `members/0006`** (`apps/members/services.py::consolidate_guardians`, migration-only code) linked every orphan Guardian to its `ParentAccount` by email (creating the account when missing), backfilled the account phone, and **merged duplicate guardians** (survivor = has `external_client_id` → most members → lowest pk; members + applications repointed before delete; `external_client_id` preserved). **Schema migration `members/0007`** drops the `email`/`phone` columns + makes `parent_account` NOT NULL. (Plan tasks 3+4 were merged: the reader proxies must flip atomically with the writers.)
  - **Single-writer collapse:** `change_parent_email` no longer mirrors to Guardian; `resolve_guardian_for_account` drops the email default; registration draft-save writes the **account** phone (the submit-time phone-sync block is deleted); approval **raises** `ValueError` instead of creating an account-less Guardian. All readers (Invoice Ninja, DocuSeal, `mark_agreement_sent`, `guardian_contact_phone`, exports) are unchanged — the proxies cover them (integration tasks gained `parent_account` to their `select_related` chains to avoid an N+1).
  - **Tests:** `tests/support.py::make_guardian` (account-linked) is the standard fixture; `consolidate_guardians` is covered via historical-schema (members.0006) migration tests. Full-repo gate: `uv run pytest -q` → **1316 passed**, ruff + mypy clean (314 source files), `makemigrations --check` clean.

- **Guardian/ParentAccount consolidation — Plan 2 (unified admin) delivered — CONSOLIDATION COMPLETE (2026-06-15)**
  - One admin entry for the parent. Plan: `docs/superpowers/plans/2026-06-15-guardian-consolidation-plan2-unified-admin.md`.
  - **Single "Vecāki" surface:** `GuardianAdmin` uses a `GuardianAdminForm` that edits the account-owned `email`/`phone`/`is_active` alongside the Guardian domain fields — `email` changes route through `change_parent_email` (uniqueness enforced; `clean_email` rejects duplicates); `has_add_permission=False` (guardians come from the registration flow); `Guardian` relabelled "Vecāks"/"Vecāki" (options-only migration `members/0008`).
  - **ParentAccount removed from the menu:** `FkAdminSite.get_app_list` filters out the `ParentAccount` model entry (the now-empty "Accounts" section disappears) while keeping it **registered** — its change/changelist URLs still resolve and account-only records remain reachable by direct URL. `ParentAccountAdmin` slimmed (the email-routing `save_model` + the related-records block removed — both now live on the Guardian page). The redundant "Vecāka konts" review cross-link is dropped.
  - Full-repo gate: `uv run pytest -q` → **1321 passed**, ruff + mypy clean (316 source files), `makemigrations --check` clean. **The Guardian/ParentAccount double-bookkeeping is fully resolved:** one source of truth for contact data (the account, read via Guardian proxies), an enforced 1:1, and one admin entry.

- **P7 audit close-out delivered — P7 DEV WORK COMPLETE (2026-06-16)**
  - The two admin mutations introduced in C-ii that weren't audited now emit `AuditEvent`s, closing the last P7 audit gap. Spec/plan: `docs/superpowers/{specs,plans}/2026-06-16-p7-audit-confirm-merge*`.
  - Two new `AuditEvent.Action` choices (`BILLING_RECORD_CONFIRMED`, `TRAINING_GROUPS_MERGED`) via one choices-only migration `core/0004`. `BillingRecordAdmin.confirm_view` records `BILLING_RECORD_CONFIRMED` on the real DRAFT→CONFIRMED transition only (not the no-op); `TrainingGroupAdmin.merge_training_groups` records `TRAINING_GROUPS_MERGED` after the atomic merge commits, with metadata `{merged_group_ids, merged_names, members_reparented}` (target = the surviving group). `record_audit_event` is fail-safe, so neither action's behaviour changed.
  - Full-repo gate: `uv run pytest -q` → **1326 passed**, ruff + mypy clean (319 source files), `makemigrations --check` clean.
  - **LAN acceptance signed off 2026-06-19 — P7 COMPLETE.** Verified on dev: unified **Vecāki** admin (merged guardians correct; email/phone/is_active edit routes via `change_parent_email`), fixed menu order with "Parent accounts" gone, all three billing-confirm paths audited, group-merge audited. **Two live-found bugs fixed during the pass:** (1) the one-click confirm + agreement quick-action buttons were `<form>`s **nested** inside the admin's changelist/change `<form>` (invalid HTML → browsers drop the inner form, so the buttons submitted the outer form) — rebuilt as bare `<button formaction=… formmethod="post">` that ride the surrounding admin form's CSRF, with `action`/`next` passed in the formaction query string; `BillingRecordAdmin.save_model` now also audits a DRAFT→CONFIRMED done via the status dropdown + Save (commit `b1d27aa`). (2) a **list-triggered** agreement quick-action now returns to the list via a validated `next` (`_after_review_redirect`); change-page disclosure forms pass no `next` and still land on the change page (commit `7b3311c`). Final gate: **1331 passed**. Out of P7 by design: account-without-guardian admin visibility; parent self-service email change with OTP.

- **P8 delivered — Agreement lifecycle (2026-06-30)**
  - Agreement states extended with `superseded` and `discontinued`. New `AgreementLifecycleEvent` model stores parent-visible lifecycle history. Spec: `docs/superpowers/specs/2026-06-30-p8-agreement-lifecycle-design.md`; plan: `docs/superpowers/plans/2026-06-30-p8-agreement-lifecycle.md`.
  - **Minor amendment:** note-only change on a signed agreement; agreement stays signed, no parent email.
  - **Material amendment:** old signed agreement becomes `superseded`/`is_current=False`; new current agreement generated and follows existing signing flow (DocuSeal or paper).
  - **Discontinuation:** signed agreement becomes `discontinued`; `Member` marked `discontinued` with effective date/reason/timestamp; parent email always sent. Staff selects invoice rows during discontinuation. Paid and partial invoices block before mutation. Unpushed/unsent invoices cancelled locally; Invoice Ninja draft invoices (`external_status=created`) cancelled locally and archived in background; sent unpaid invoices (`external_status=sent`) cancelled locally and cancelled in Invoice Ninja in background.
  - **Invoice Ninja credit-note live validation** against sandbox (`docs/p8_invoice_ninja_credit_validation.md`): credit create works with positive `cost`; auto-apply via `/credits/bulk action=apply` returns 422 (unsupported). App sets `requires_staff_apply=True`; staff must manually apply credit in Invoice Ninja until a supported apply endpoint is discovered.
  - Admin: agreement module has minor/material/discontinue actions; `BillingAdjustment` admin has retry/status controls. Parent portal shows lifecycle status and history. New audit events for lifecycle and credit actions.
  - Full-repo gate: `uv run pytest -q` → **1517 passed**, `uv run ruff check .` → clean, `uv run mypy .` → clean, `makemigrations --check` → no changes.

- **Kit-size collapse (2026-07-07)** — collapses the former two-field parent-flow kit-size model (shirt + shorts) into a single canonical field. `member_kit_size_shirt` is the canonical kit-size field; `member_kit_size_shorts` is retained in the database as legacy-only (never written by current parent flow, no migration added). Field label changed from "Krekla izmērs" to "Formas izmērs". Removed `member_kit_size_shorts` from form sections, step-gating, and `_field_step_map`; replaced separate shirt+shorts choice population with a single `kit_size_sort_key`-sorted list of active `KitSizeOption.Kind.SHIRT` rows. Added `_KIT_SIZE_ORDER` map and `kit_size_sort_key(label: str) -> tuple[int, int | str]` helper in `apps/members/models.py` (numeric labels sort first by value; then XXS=0 → 5XL=9; unknown labels sort alphabetically case-insensitive). Submit validation now requires only `member_kit_size_shirt_id`. Admin-side shirt/shorts cleanup is deferred to P11 family hub. Full-repo gate: `uv run pytest -q` → **1581 passed**, `uv run ruff check .` → clean, `uv run mypy .` → success (377 source files), `makemigrations --check` → no changes. Spec: `docs/superpowers/specs/2026-07-07-kit-size-collapse-design.md`; plan: `docs/superpowers/plans/2026-07-07-kit-size-collapse.md`.

- **P10 delivered — public-site analytics + registration funnel (2026-07-08)** — adds a small `apps.analytics` boundary (`config`, `sanitize`, `providers`, `services`, `{% analytics_browser %}` tag) with `stub` + Plausible-first provider modes; independent browser/server flags; parent-only browser script injection; fixed CTA/empty/error/validation hooks; server milestone events (`registration_start`, `email_verified`, `application_submitted`); and URL referral attribution (`/register/?ref=coach-a`, sanitized `[a-z0-9_-]`, max 64, single-use session, persisted as `RegistrationApplication.referral_code`, safe `referral_code` event prop). Admin pages are not tracked; analytics payloads are allowlisted and exclude PII/model PKs/free-text/document data. Docs: `docs/analytics.md`. Verification: targeted analytics tests **38 passed**, `makemigrations --check` clean, ruff clean, mypy clean; fast lane before final referral guard **1360 passed**. Live provider/dashboard smoke is still required before production enablement.

- **P7 Slice B delivered — CSV export (2026-06-13)**
  - Staff-only, audited CSV export of **Members** and **Registration applications** via Django admin changelist actions. Spec: `docs/superpowers/specs/2026-06-13-p7-csv-export-design.md`; plan: `docs/superpowers/plans/2026-06-13-p7-csv-export.md`. Slice C (admin search/filter + document-UX + sync-health visibility) remains.
  - **`apps.core.export.csv_response(filename, columns, rows)`** — UTF-8 **BOM** + **`;`** delimiter (so Latvian Excel opens it with diacritics + columns intact on double-click), value formatter (`None→""`, `bool→jā/nē`, dates ISO), and a **spreadsheet formula-injection guard** (prefixes a `'` to cells starting `= + - @` / tab / CR so user-supplied names/addresses can't execute in Excel).
  - **Two actions per model** (on `MemberAdmin` + `RegistrationApplicationAdmin`): `export_csv` (safe columns, all staff) and `export_csv_with_sensitive` (safe + sensitive, **superuser-only** — hidden via `get_actions` + a defensive `is_superuser` check that refuses + records nothing). Column/row builders live in `apps/members/exports.py` + `apps/registrations/exports.py` (pure, unit-tested). Registrations export reads via the guardian accessors (admin already `select_related`s guardian + parent_account).
  - **Safe columns** carry no personal IDs / contact / address; **sensitive** adds member personal_id + address + guardian email/phone/personal_id/address. Every export records a **`DATA_EXPORTED`** audit event (new `AuditEvent.Action` value, migration `core/0003`) with `metadata={count, sensitive, format}` — **never the exported rows**.
  - Full-repo gate: `uv run pytest -q` → **1247 passed**, ruff + mypy clean (282 source files), `makemigrations --check` clean. Internal admin feature; covered by the suite (no LAN acceptance needed).

- **P7 Slice A delivered — audit baseline (2026-06-13)**
  - First slice of P7 (admin ops / export / audit). Slice B (CSV export) delivered (above); Slice C (admin search/filter + document-UX + sync-health visibility) remains. Spec: `docs/superpowers/specs/2026-06-13-p7-audit-baseline-design.md`; plan: `docs/superpowers/plans/2026-06-13-p7-audit-baseline.md`.
  - **`apps.core.models.AuditEvent`** — append-only (no `updated_at`), `actor` FK (null=system, `SET_NULL`) + `actor_label` snapshot, `action` (nested `Action` TextChoices), denormalized `target_type`/`target_id`/`target_repr` (survives target deletion), redacted `metadata` JSON, `ip_address`/`user_agent` (request-originated only), indexed `created_at`. Core's first migration (`core/0001_initial`).
  - **`apps.core.audit.record_audit_event(...)`** — single write path, called explicitly at each action site. **Fail-safe**: the whole body is wrapped in `try/except` + logged, so an audit-write error can never raise into or roll back the audited business action. Derives target from a model instance; extracts client IP (first `X-Forwarded-For` hop) + truncated user-agent from a request; falls back to `request.user`. Actions are passed as `str(AuditEvent.Action.X)` (no django-stubs plugin → keeps mypy clean).
  - **Wired events:** review actions (`application_approved`/`_rejected`/`_fix_requested`, staff), `training_group_assigned`/`_cleared`, document `previewed`/`downloaded`/`deleted` (IP/UA; delete covers both the soft-delete-on-replace path — parent label — and staff admin hard-delete), agreement `sent`/`signed`(webhook=system)/`voided`/`sync_failed`, and billing `billing_push_triggered`/`payment_sync_triggered` (staff admin actions) + `invoice_push_failed`/`invoice_send_failed`/`payment_sync_failed` (system). **Routine automated sync successes are deliberately NOT audited** — only state changes, staff-triggered actions, and failures.
  - **Read-only admin viewer** (`AuditEventAdmin`): add/change/delete all denied; list_filter (action/target_type/date), search, `date_hierarchy`. Append-only even for superusers via admin.
  - **Retention:** `AUDIT_RETENTION_DAYS` (env, default **730**) + `AUDIT_PRUNE_HOUR` (default 2); `apps.core.tasks.prune_audit_events()` deletes events older than the cutoff; registered as the daily django-q `Schedule` `audit-retention-prune` (`core/0002`, mirrors `billing/0005`). `.env.example` updated.
  - **Redaction:** `metadata`/`target_repr` carry only statuses, error codes, kinds, group names, PKs — never personal IDs, document contents, email bodies, or tokens. `actor_label` stores the actor's identifying email (staff and parent) by design (the durable identity snapshot, per spec §4) + `ip_address` for forensic value; both bounded by the retention prune.
  - **Operator guide:** `docs/audit-log.md` — how staff read the trail (Django admin → Core → Audit events), filter/search, the event catalog, what is/isn't stored, retention, and shell queries.
  - Full-repo gate: `uv run pytest -q` → **1231 passed**, `uv run ruff check .` → clean, `uv run mypy .` → clean (275 source files). No LAN acceptance needed (internal admin/log feature; covered by the suite).

- **P6 Slice A delivered — local billing domain + sibling-discount engine (2026-06-07)**
  - LOCAL ONLY: no Invoice Ninja calls. The Invoice Ninja adapter + admin-confirmed push is Slice B; payment read-back + sync health is Slice C.
  - New `apps/billing/` app. `MembershipPlan` (staff-editable: name, season, currency, annual_amount, sibling_discount_percent, installment_count, first_installment_month, is_active — exactly one active row by convention, not a DB constraint, so a next-season plan can be staged inactive). `BillingRecord` (one per `(member, season)` via `UniqueConstraint`; money fields snapshotted at creation so a later plan edit never silently mutates an existing draft; `Status` draft/confirmed + `PaymentMode` upfront/installments TextChoices with Latvian labels; `final_amount` = `manual_amount_override` when set, else `base_amount − discount_amount`). Migrations `0001_initial` (plan) + `0002_billingrecord`.
  - Pure sibling-discount engine in `apps/billing/services.py`: `compute_billing_amounts(member, plan)` returns a frozen `BillingAmounts` dataclass. Discount derives from the member's guardian (`Member.guardian.members` ordered by pk — the earliest-created child is full price, later children get `sibling_discount_percent`). Opt-out via the source application's `support_club_instead_of_multi_child_discount` forces full price. `derive_installment_schedule(plan, total)` splits into N equal monthly entries (last absorbs the rounding remainder; months wrap past December; year from `plan.season`). Money math quantizes to cents with ROUND_HALF_UP.
  - `agreement_signed` signal (`apps/agreements/signals.py`) emitted at the end of `mark_agreement_signed` (after save + email). Billing connects a receiver in `BillingConfig.ready()` (`apps/billing/signals.py`, `dispatch_uid="billing.on_agreement_signed"`) so signing auto-creates a DRAFT `BillingRecord` via `create_draft_billing_for_member` (idempotent get_or_create on `(member, season)`; no-ops + logs a warning when there is no active plan — never raises). Dependency direction is billing → agreements.
  - `recompute_billing_record(record)` re-derives a DRAFT record's amounts from a fresh DB copy of its plan (no-op on confirmed; honours a manual override). Admin (`apps/billing/admin.py`): `MembershipPlanAdmin` + `BillingRecordAdmin` with read-only computed fields, editable `manual_amount_override`/`manual_override_reason`/`status`, a `Vecāks` guardian column, and a `Pārrēķināt no plāna` action that recomputes only DRAFT records.
  - `preferred_payment_mode` registration field: `RegistrationApplication.preferred_payment_mode` (`PaymentMode` choices, blank; migration `0008`), `Maksājuma veids` form field in the agreement section, persisted in services, and added to `MANUAL_P1_FIELDS` (provenance). Opt-out reuses the existing `support_club_instead_of_multi_child_discount` field — no new opt-out field.
  - `backfill_billing` management command creates draft records for already-SIGNED agreements that lack one (dedupes by member; idempotent via the service). Latvian admin copy in `apps/billing/messages.py`; `billing_summary_line` admin summary in `apps/billing/presentation.py`.
  - Full-repo verification after Slice A: `uv run pytest -q` → **1053 passed** (incl. a post-review recompute-override test), `uv run ruff check .` → clean, `uv run mypy .` → clean (219 source files).
  - **Manual LAN smoke confirmed + signed off 2026-06-07** on `http://192.168.3.245:8000` (7/7 acceptance items): activate plan in admin; sign an agreement via the review-detail `Atzīmēt kā parakstītu` button → draft `BillingRecord` auto-created at the right amount; billing admin surface (guardian `Vecāks` column, read-only computed fields vs editable override/status); `Pārrēķināt no plāna` updates a draft after a plan edit **and** skips a confirmed record; `backfill_billing` populates a pre-existing signed agreement and is idempotent on re-run; the parent `Maksājuma veids` field renders and persists (`upfront`). Sibling-discount + opt-out verified via the green `tests/billing/test_discount_engine.py` unit suite rather than a second live registration (guardian de-duplication would confound the browser path). ~~**Known cosmetic wart (not a data bug):** `backfill_billing` prints `"N created"` counting already-existing records…~~ **Resolved in P6 Slice C** (`235c49d`): the command now reports an honest created-count via a row-count delta.
  - Plan: `docs/superpowers/plans/2026-06-07-p6-slice-a-billing-domain.md`. Design: `docs/superpowers/specs/2026-06-07-p6-slice-a-billing-domain-design.md`.

- **P6 Slice B delivered — Invoice Ninja push integration (2026-06-08)**
  - Push-only and admin-confirmed: Django → Invoice Ninja. Payment read-back, webhooks, and void/regenerate remain Slice C. Mirrors the DocuSeal integration shape.
  - New boundary `apps/integrations/invoice_platform.py` (exception taxonomy `InvoicePlatformError` + `Config`/`Auth`/`NotFound`/`Transient`; frozen `ProductResult`/`ClientResult`/`InvoiceResult`; stub/`invoiceninja` dispatch on `settings.INVOICE_PROVIDER_MODE`) + concrete `apps/integrations/invoice_ninja.py` provider (HTTP via `requests`, `X-Api-Token` header, `_request` status→exception map incl. **429→Transient**, `_unwrap` list-safe `{"data":…}` unwrap, payload builders, duplicate-invoice-number idempotency recovery via `_find_invoice_id_by_number`). Settings: `INVOICE_PROVIDER_MODE` (default `stub`), `INVOICE_NINJA_API_URL/KEY`, `INVOICE_NINJA_NUMBER_PREFIX` (default `MMS`).
  - Data model: `Guardian.external_client_id` (migration `members/0003`), `MembershipPlan.external_product_id`, `BillingRecord.external_status`/`external_error_code`, new `BillingInvoice` model (one row per installment; `(billing_record, sequence)` unique; holds `external_invoice_id`/`external_status`/`external_error_code`) (migration `billing/0003`). `product_key` is a derived helper (`membership_plan_product_key`), not a stored column.
  - Mapping (locked in the design): Guardian → IN Client, MembershipPlan → IN Product (mirrored, referenced by `product_key`), each child's BillingRecord → its own invoice stream (no sibling consolidation), upfront = 1 invoice / installments = one IN invoice per `derive_installment_schedule()` row, net amount per line with the sibling discount shown as a Latvian note (not IN's native discount field).
  - Flow: `apps/integrations/tasks.py::push_billing_record` (django-q2) ensures product → ensures client (Guardian saved without `updated_at` — it has none) → `materialize_installments` → creates one invoice per row lacking an external id → rolls up `external_status` to `synced`/`failed`. Deterministic numbers `{INVOICE_NINJA_NUMBER_PREFIX}-{record}-{seq}` anchor idempotency (re-push skips rows already created; an already-`synced` record is a clean no-op). Transient errors raise `RetryableInvoiceError` (cluster retries); terminal errors persist `failed` + Latvian-mapped code (`apps/billing/messages.py::get_invoice_error_message`) and return.
  - Trigger: `BillingRecordAdmin` action **"Izrakstīt rēķinus (Invoice Ninja)"** — confirmed-only (drafts skipped with a Latvian warning); `external_status` surfaced in list/filter/readonly. The per-plan "sync product" action was intentionally omitted (the push job ensures the product).
  - Full-repo gate after Slice B: `uv run pytest -q` → **1080 passed**, `uv run ruff check .` → clean, `uv run mypy .` → clean (230 source files).
  - **Manual LAN smoke confirmed + signed off 2026-06-08** on `http://192.168.3.245:8000` in `stub` provider mode (no live Invoice Ninja instance), with the django-q worker running: confirm a `BillingRecord` → `Izrakstīt rēķinus (Invoice Ninja)` action → `external_status` becomes `synced` with 10 `BillingInvoice` rows (€40×10=€400), the plan/guardian get their stub external ids; the real-IN payload builder verified directly (number `MMS-1-3`, product_key `biedra-maksa-2027`, Latvian line label, net `cost`); idempotent re-push (no duplicate rows); a draft is skipped with the Latvian "vispirms apstipriniet" warning. **LAN-feedback refinement (committed during testing):** the admin action now skips already-`synced` records and reports them separately (`Jau izrakstīti: N`) instead of counting a no-op re-push as "pushed", while still re-enqueuing `failed`/unpushed confirmed records for retry. A true end-to-end run against a live Invoice Ninja instance (`INVOICE_PROVIDER_MODE=invoiceninja`) remains for whenever one is provisioned — see also the deferred crash-recovery hardening below.
  - **Deferred to Slice C (hardening, flagged in final review, not blocking) — RESOLVED in Slice C:** `ensure_product`/`ensure_client` had a narrow crash-window dedup gap — if the IN POST succeeded but the Django `save()` of the external id failed, a retry could create a duplicate product/client in IN. Closed in Slice C: `ensure_product` looks up by `product_key` first; `ensure_client` sends + looks up by `custom_value1=guardian.pk` first.
  - Plan: `docs/superpowers/plans/2026-06-07-p6-slice-b-invoice-ninja-push.md`. Design: `docs/superpowers/specs/2026-06-07-p6-slice-b-invoice-ninja-push-design.md`.

- **P6 Slice C delivered — payment read-back + scheduled sync + sync health (2026-06-08)**
  - Closes P6 acceptance items 7, 8, 9 and verifies 1, 2. Payment read-back from Invoice Ninja into Django (push → read-back loop now complete). IN remains the source of truth; Django mirrors a read-only payment projection. **Live-IN end-to-end validation is the remaining sign-off step (Task 13) — the IN instance now exists, so this validates both the Slice B push path (only stub-verified before) and the new read-back path.**
  - Data model (migration `billing/0004`): `PaymentStatus` TextChoices (`unpaid`/`partial`/`paid`, Latvian labels). `BillingInvoice` gains `payment_status`, `paid_to_date`, `balance` (nullable), `last_payment_date` (nullable), `last_synced_at`. `BillingRecord` gains a rolled-up `payment_status` + `payment_synced_at`, plus a dedicated `payment_error_code` (migration `billing/0006`) — kept separate from the push-side `external_error_code` so a successful read-back never erases a push-failure signal.
  - Adapter/provider: `invoice_platform.fetch_invoice_payment(external_invoice_id) -> PaymentResult` (frozen dataclass: external_invoice_id, payment_status, amount, paid_to_date, balance, last_payment_date); stub returns a deterministic `unpaid` projection. `invoice_ninja.fetch_invoice_payment` does `GET /invoices/{id}`, maps IN `status_id` (4→paid, 3→partial, else unpaid) with an amount-derived fallback (`paid>0 and balance==0`→paid; `paid>0`→partial), extracts the latest payment date from the `payments` array. **The exact IN field names/payment-date location are design-intent pending live confirmation in Task 13 (P3/P5 stub-hides-shape lesson).**
  - Slice-B dedup hardening folded in (see resolved note above): `ensure_product` lookup-by-`product_key`, `ensure_client` lookup-by-`custom_value1=guardian.pk` before create.
  - Jobs (`apps/integrations/tasks.py`): `sync_billing_payments()` — nightly batch sweep over all `BillingInvoice` rows with an external id; **per-row errors are logged and skipped (one bad row never aborts the run)**; rolls up `payment_status`/`payment_synced_at` only on touched records via `roll_up_payment_status` (`apps/billing/services.py`). `sync_billing_record_payments(record_id)` — manual single-record sync; surfaces a terminal error on `BillingRecord.payment_error_code` (its own field, cleared on success), re-raises `RetryableInvoiceError` on transient so the cluster retries. `enqueue_sync_billing_record_payments` mirrors the push enqueue helper.
  - Scheduling: idempotent data migration `billing/0005` registers a django-q2 `Schedule` (`name="billing-payment-sync"`, `func=apps.integrations.tasks.sync_billing_payments`, `schedule_type=DAILY`); the running `qcluster` fires it. New setting `BILLING_PAYMENT_SYNC_HOUR` (default 3) feeds the initial `next_run`; the `Schedule` row stays editable in Django admin → configurable cadence (item 9).
  - **Pre-existing TZ gap fixed in this slice:** the project previously set neither `TIME_ZONE` nor `USE_TZ` (Django default `America/Chicago`), so `_next_run()`'s `timezone.localtime()` would have fired the sweep at the configured hour in Chicago time (≈11:00 Riga for hour=3). Now `settings.py` sets `TIME_ZONE = os.environ.get("TIME_ZONE") or "Europe/Riga"` + `USE_TZ = True`, so the nightly hour is interpreted in local (Riga) time and admin renders local time correctly. Datetimes remain stored in UTC. NB: a `billing-payment-sync` `Schedule` row created before this fix keeps its original next_run time-of-day — adjust it in Django admin (or delete it and re-create via the migration on a fresh DB) if it was created under the old default.
  - Admin: `BillingRecordAdmin` gains a confirmed-only **"Pārbaudīt maksājumus (Invoice Ninja)"** action (drafts skipped with a Latvian warning), `payment_status`/`payment_synced_at` columns + `payment_status` list-filter + read-only fields, and a read-only `BillingInvoiceInline` exposing per-installment payment fields. Push-side sync health/retry from Slice B unchanged. `PAYMENT_STATUS_LABELS` added to `apps/billing/messages.py`.
  - Trigger verification (items 1–2): `tests/billing/test_signed_completed_trigger.py` pins that an electronic agreement reaching `signed` (the "completed" final state) auto-creates a draft `BillingRecord` via the existing `agreement_signed` signal — i.e. "signed = completed". No trigger rebuild.
  - Cosmetic fix: `backfill_billing` now reports an honest created-count (row delta) instead of over-counting pre-existing matching records.
  - Full-repo gate after Slice C: `uv run pytest -q` → **1114 passed**, `uv run ruff check .` → clean, `uv run mypy .` → clean (240 source files).
  - **Manual LAN smoke confirmed 2026-06-09** on `http://192.168.3.245:8000` in `stub` provider mode, with the django-q worker running: admin payment columns + `payment_status` filter + read-only `BillingInvoiceInline` render; the confirmed-only **"Pārbaudīt maksājumus (Invoice Ninja)"** action enqueued + the worker processed `sync_billing_record_payments` (no failures) → all 10 `BillingInvoice` rows got `last_synced_at` and the record rolled up to `payment_status="unpaid"` + `payment_synced_at` set + `payment_error_code` clean (stub returns unpaid — paid/partial variety needs the live-IN run); the draft record was skipped with the Latvian warning; the `billing-payment-sync` `Schedule` row shows Daily at **03:00 Riga (00:00 UTC)**, confirming the TZ fix.
  - **Live Invoice Ninja validation (2026-06-09)** against a real instance (`in.mplytics.eu`), driven from the working tree — surfaced bugs the stub hid (the P3/P5 pattern again). All fixed and re-verified live:
    - `_request` lacked `X-Requested-With: XMLHttpRequest` + `Accept: application/json`; without them IN returns **200 + SPA HTML** on a failed request, so error handling never sees a 4xx and JSON parsing blows up. Added both headers.
    - Duplicate-invoice-number recovery keyed on `"invoice number"`, but IN's real 422 is `{"errors":{"number":["The number has already been taken."]}}` — changed the trigger to `"already been taken"`. Verified the live re-push recovers the existing invoice id and rolls up to `synced`.
    - `fetch_invoice_payment` now GETs `?include=payments` — IN does not embed payments on the invoice by default, so `last_payment_date` was always None. Confirmed live: full payment → `paid` (paid_to_date/balance/date all correct), partial → `partial`, via the `payments[].date` field.
    - **Product-note pollution (user-reported):** IN's "Update Products" setting copies an invoice **line's** notes onto the shared catalog product when `product_key` matches. The member-specific line note was polluting the product (`"Biedra maksa — <member> — 2027"`). Fix: the line note is now generic (`product_name`), and the per-member + sibling-discount detail moved to the invoice **`public_notes`**. Verified live: after a generic-note invoice, the product note stays/reverts to `"Biedra maksa 2027"`.
    - Push path otherwise validated live end-to-end: product + client created (client carries `custom_value1=guardian.pk`), `ensure_product` dedup-reuse confirmed, 10 invoices created, idempotent re-push.
    - **Dedup lookups reused archived/soft-deleted rows (found after wiping IN test data):** IN's `?filter=` and default list endpoints return archived + soft-deleted rows, so after the IN test data was deleted the lookup resurrected a deleted client/product id and `create_invoice` failed (`misconfigured` — "client id is invalid"). Fix (`636ba07`): lookups now pass `?status=active` and skip rows flagged `is_deleted`/`archived_at` (clients, products, and the duplicate-number invoice recovery, which also now verifies the number matches). Verified live: a re-push after a full IN wipe creates fresh active records and syncs 10/10. **Operational note:** wiping/deleting records directly in Invoice Ninja leaves stale `guardian.external_client_id` / `MembershipPlan.external_product_id` / `BillingInvoice.external_invoice_id` in Django. The push job now self-heals the client-id case: if `create_invoice` returns a `client_id is invalid` error, the stale `Guardian.external_client_id` is cleared, `_ensure_client_id` re-resolves an active client, and the invoice create retries once. Product-id and invoice-id stale references still require manual clearing of the corresponding Django `external_*` ids (e.g. the billing reset used in dev) — those paths are not production flows.
    - **Client/product dedup returned the wrong row (found during UI test):** Invoice Ninja **ignores** `?custom_value1=` / `?product_key=` filters and returns *every* row, so `_find_client_id_by_pk`/`_find_product_id_by_key` (Task 4) were reusing an arbitrary `rows[0]` — mapping every guardian's invoices onto one manually-created client. Fix (`cb7ff36`): narrow with the fuzzy `?filter=` (which DOES search custom fields — verified: `custom_value1` persists and is searchable) **then exact-match `custom_value1`/`product_key` client-side**, creating fresh when nothing truly matches. Verified live: each guardian now gets its own client (`custom_value1=pk`, correct email), idempotent on retry; a fresh record push lands invoices on the correct client. **Caveat:** Invoice Ninja makes an invoice's `client_id` immutable (PUT → 422), so invoices created on the wrong client by the buggy code cannot be reassigned — they must be deleted/recreated or wiped as test data. Regression tests assert non-matching rows are NOT reused.
    - Test hardening: `tests/conftest.py` now forces `INVOICE_PROVIDER_MODE`/`AGREEMENT_PROVIDER_MODE`/`OCR_PROVIDER_MODE` to `stub` (via `setdefault`, before settings load `.env` with `override=False`), so a developer's live `.env` creds never make the suite hit external services. Full gate stays green (1115) with live creds present.
  - **Deferred (user decision, 2026-06-09):** invoices are pushed in IN **Draft** status (`status_id=1`) — invisible to the guardian until "sent". Auto-issuing (or a staff review/send step) is deferred to a later slice pending the issue/send policy. Tracked in `docs/milestones.md`.
  - Full-repo gate after the live fixes: `uv run pytest -q` → **1115 passed**, ruff + mypy clean. Remaining for P6 close-out: a confirmation pass on the deployed cloud `:dev` artifact (container + `qcluster` service + migrate-on-deploy at production fidelity).
  - Plan: `docs/superpowers/plans/2026-06-08-p6-slice-c-payment-readback-sync-health.md`. Design: `docs/superpowers/specs/2026-06-08-p6-slice-c-payment-readback-sync-health-design.md`.

- **P6 installment calendar + per-plan due day (2026-06-09)** — completes P6 acceptance item 6 ("agreed installment calendar … billing start month respected"), surfaced during live validation.
  - `MembershipPlan` gains `payment_due_day` (PositiveSmallInteger, default **20**, 1–31 validated) and `skip_months` (CharField, default **"7,12"** = July + December) + a `skip_months_list` parsing property (tolerant: trims, drops non-numeric/out-of-range, sorted-unique). Migration `billing/0007`.
  - `derive_installment_schedule` now places `installment_count` **real** installments on successive months starting at `first_installment_month`, **skipping** any month in `skip_months_list` (so e.g. start=Jan, skip Jul+Dec, count=10 → Jan–Jun + Aug–Nov), with each due date on `payment_due_day` **clamped to the month length** (`min(due_day, monthrange)`), year-wrap preserved. Per-installment guard raises `ValueError` if 12 consecutive months are skipped (all-skipped misconfig). Decision: chose N real installments over the 12-invoices-with-€0-on-Jul/Dec alternative; amount split (€300 ÷ count, last absorbs remainder) unchanged.
  - `MembershipPlanAdmin` surfaces `payment_due_day` in the list; both new fields editable on the change form.
  - **Go-forward only:** already-materialized `BillingInvoice` rows (and invoices already in IN) keep their prior schedule; `materialize_installments` is idempotent. To get the Jan–Jun + Aug–Nov calendar, set the plan's `first_installment_month=1` in admin (data config, not code).
  - Gate: `uv run pytest -q` → **1123 passed**, ruff + mypy clean. Plan: `docs/superpowers/plans/2026-06-09-p6-installment-calendar-due-day.md`.

### Test suite consolidation (2026-05-24)
- `tests/` reduced from 18,277 LOC / 807 tests / 153 s → **16,276 LOC / 762 tests / 84 s** (−11% LOC, −45% runtime). Plan: `docs/superpowers/plans/2026-05-24-test-suite-consolidation.md`.
- Shared fixture homes (use these for new tests instead of repeating per-test bootstrap):
  - `tests/conftest.py` — cross-app: `parent_account`, `other_parent_account`, `verified_client`, `other_verified_client`, `staff_client` (renamed from `admin_client` to avoid shadowing pytest-django's built-in).
  - `tests/registrations/conftest.py` — registrations-scoped: `guardian_identity_file`, `member_identity_file`, `member_portrait_file`, `kit_sizes`, `submit_payload`, `draft_application`, `draft_with_documents`, `submitted_application`, `fix_requested_application`, `rejected_application`.
- Visual / template / CSS contract for parent pages lives in **`tests/registrations/test_visual_contract.py`** (consolidated from the former `test_parent_visual_pages.py` and `test_task2_logo_and_css.py` + the visual half of `test_verified_registration_entry.py`). Add new visual assertions here, not in workflow / permissions test files.
- Removed files: `test_parent_visual_pages.py`, `test_task2_logo_and_css.py`, `test_personal_data_consent_schema.py`, `test_ocr_prefill_vs_suggestion.py`, `test_p3_remaining_gaps.py`. Their behaviors are preserved in the natural-home files listed above (or were duplicates of existing coverage).

### Task 6 follow-up debt
- Revisit desktop typography in Task 6 UI pass: blue text renders too heavy/thick on desktop and needs refinement.
- Django admin document UX should distinguish active vs replaced (soft-deleted) documents and hide or clearly disable preview/download actions for replaced rows.
- Training group assignment on approval (currently left empty).
- Admin activity audit entries for review actions.
- **DOB-driven member-form prefill.** Normalizer now surfaces `data.date_of_birth` (validated live on 2026-05-22), so `DocumentExtraction.summary` carries DOB. Outstanding: wire OCR-derived DOB into the member form prefill with the same source-badge treatment as other identity fields.
- **Surname normalization for Latvian IDs.** Live validation showed `data.first_surname` alone does not always match the manifest last name (likely diacritic / two-surname cases on guardian samples); consider folding `data.second_surname` and harmonizing diacritics before scoring downstream.

### Approved design and research direction (2026-05-05)
- **Build now:** whole-app visual system and registration form redesign (major parent-flow changes allowed).
- **Registration entry direction:** implemented in P1 as guardian email entry with one-time email code verification, verified continuation, guardian-only prefill, and chooser/dashboard for existing guardians. See `docs/superpowers/specs/2026-05-08-p1-field-contract-and-verified-registration-gate-design.md`.
- **Security fix — parent identity verification:** implemented in P1. Typed email in registration draft is a claim, not proof of ownership. Verified access now gates registration continuation and portal access. See `docs/superpowers/specs/2026-05-08-p1-field-contract-and-verified-registration-gate-design.md`.
- **Research spikes / preferred directions:** OCR vendor direction narrowed to **tiny-IDP** only, agreement generation with manual signing first and **DocuSeal self-hosted** favored for future richer processing, and SMTP/email provider strategy for scale.
- **Hosting stance:** self-hosted is not assumed more secure by default; compare self-hosted and SaaS by security posture, ops maturity, compliance, and API portability.
- **Visual direction:** unified design system, calm centered parent flow, denser admin shell, club logo hero-style on parent entry screens.
- **Style source of truth:** `style-guide/` supersedes `design-template.html`. Canonical tokens currently: font `Anton`, blue `#0f0851`, red `#ce1c20`.
- **Agreement handling, first slice:** after admin approval, generate agreement, allow manual signing outside platform (LV qualified electronic signature or paper), then mark signed and optionally upload signed copy. Richer countersign/order automation may follow later.
- **GDPR/EU compliance mandatory** for all third-party integrations.
- **Service boundary:** self-hosted services may live in separate infrastructure/Ansible projects; this repo should integrate loosely via adapters and external config, not own their deployment lifecycle.
- Spec: `docs/superpowers/specs/2026-05-08-fk-cesis-mms-product-spec.md`.

Reference docs:
- Canonical product spec: `docs/superpowers/specs/2026-05-08-fk-cesis-mms-product-spec.md`
- Milestones: `docs/milestones.md`
- Style guide assets: `style-guide/`
- Style guide tokens: `style-guide/tokens.md`, `style-guide/tokens.css`
- Design template (exploratory only, superseded by `style-guide/` on conflict): `design-template.html`

## Milestones
- `M1` — Foundation and security baseline
- `M2` — Parent registration intake
- `M3` — Admin review and member creation
- `M4` — Billing and Invoice Ninja sync
- `M5` — Admin operations and export
- `M6` — Production readiness

Use `docs/milestones.md` as authoritative milestone tracker and base for future development tasks. Keep it updated as scope/status changes.

Archive rule:
- `docs/archive/` is historical only.
- Do not use archived docs for planning, execution, or status by default.
- Read archived docs only when user explicitly asks for history/archive context.

## Commands
```bash
uv sync                                # install deps
uv run python manage.py migrate        # run migrations
uv run python manage.py runserver      # start dev server locally
uv run python manage.py qcluster       # start background-job worker (P3.5+)
uv run pytest                          # run test suite
uv run pytest -q -m "not slow"         # fast lane (PR CI)
uv run ruff check .                    # lint
uv run mypy .                          # type check

Testing lanes:
- Fast local/PR lane: `uv run pytest -q -m "not slow"`
- Full suite: `uv run pytest -q`
- Marker rules + lane contract live in `docs/testing.md`.

# Container local smoke (deployed runtime lives in fk-cesis; see docs/local-docker-smoke.md)
docker build -t fk-cesis-mms:dev .     # build the production image locally
docker compose up -d                   # local smoke test (web + qcluster + postgres on 127.0.0.1:8000)
docker compose logs -f web qcluster    # tail app + worker logs
docker compose down -v                 # tear down stack and named volumes
```

Rules:
- Always use `uv run` for Python commands.
- Do not assume `venv/` or `pip` exist.
- For user-accessible dev servers, expose app through `kimaki tunnel`, not localhost-only.
- For acceptance testing, expose usable app slices early, not only at end.

### Background worker (django-q2)

The web process and the worker process must both be running in local dev — they're separate terminals.

- **Start:** `uv run python manage.py qcluster` (separate terminal from `runserver`)
- **Broker:** Django ORM (`Q_CLUSTER.orm = "default"`) — no Redis needed
- **Worker env:** the qcluster process must have the same `.env` as the web process (specifically `OCR_ENCRYPTION_KEY`, `TINY_IDP_API_URL`, `TINY_IDP_API_KEY`). Missing keys cause job failures inside the worker, not at boot.
- **Retry policy:** `Q_CLUSTER.max_attempts = 2`. Jobs that *raise* are retried once; jobs that return normally (including OCR jobs that persist a classified FAILED state) are not retried.
- **Inspection:** Django admin → "Failed tasks" and "Successful tasks" (django_q models). For ad-hoc inspection: `uv run python manage.py shell` then `from django_q.models import Task; Task.objects.filter(success=False)`.
- **Tests:** `tests/conftest.py` sets `Q_CLUSTER_SYNC=1` so jobs run in-process during pytest. No real cluster needed for the test suite.
- **Jobs registered:**
  - `apps.integrations.tasks.ocr_extract_job(document_id)` — runs OCR via the existing `safe_extract_document_data` wrapper, persists `DocumentExtraction` + updates `Document.ocr_status` + tags `field_sources`. Enqueued by `apps.integrations.tasks.enqueue_ocr_job(document_id)`.
  - Retry semantics: raises `RetryableOCRError` for transient classified codes (`request_timeout`, `provider_unavailable`, `rate_limited`) so django-q2 retries once. Terminal codes (`auth_failed`, `invalid_response`, `provider_misconfigured`) persist FAILED and return — no retry.

## Coding Conventions
- **TDD first** — write failing test, then implementation, then verify.
- **Plan before coding** — multi-step work needs written plan.
- **Verify before completion** — run `uv run pytest -q && uv run ruff check . && uv run mypy .` before claiming done.
- Use `apps/<domain>/` layout; each app should eventually contain `models.py`, `services.py`, `views.py`, `urls.py`.
- Business rules live in `services.py` / `rules.py`, not views or templates.
- No sensitive PII in logs. Mask personal IDs; redact external API payloads.
- All external API calls (Invoice Ninja, OCR) run through background jobs with retry state.
- **Two-channel branch strategy (2026-05-26+):** all new development lands on `dev`. `main` is the release branch — merged into from `dev` via PR only after verification on the dev server. Every push to `dev` rebuilds the floating GHCR `:dev` image; every push to `main` rebuilds GHCR `:main` *and* an immutable `:<major>.<minor>` version tag (`<major>` from `VERSION` file, `<minor>` resets on major bump then auto-increments per commit). Dev server tracks `:dev`; prod server tracks `:main` with the option to pin to a `:<X.Y>` for rollback.
- Keep commits small and verifiable; never commit directly to `main` (open a PR from `dev`).
- When a worktree is explicitly requested, create it inside the project (for example `.worktrees/`), copy the project-root `.env` in, and (if exposed via a tunnel) update `SITE_URL` and related trusted-origin settings in the worktree `.env` so CSRF-protected forms work.
- Current acceptance-test baseline uses LAN bind on `192.168.3.245:8000`.
- Ask before major structural changes or architecture changes.
- Keep context lean; read only files needed for current task.
- Keep `README.md` and project docs accurate when architecture or workflows change.

## Security Rules (PII / Documents)
- Registration identity documents stored under `PRIVATE_DOCUMENTS_ROOT` (`private-uploads/`), separate from `MEDIA_ROOT`.
- OCR-extracted document metadata (number, issuer, issuance date, expiry, etc.) is sensitive data and must be protected with same posture as underlying identity documents.
- No public file URLs for registration documents. Every preview/download passes through admin-only Django views that enforce staff authorization.
- Identity documents stored in private storage; streamed through authenticated backend views.
- No public file URLs. Every download checks application/member authorization.
- Personal IDs masked in list/search; full values only on restricted detail views.
- Magic links: single-use, short TTL, revoked after use, rate-limited.
- Document view/download/delete actions audited via `AuditEvent`.
- Secrets stored outside repo (`.env`); never committed.

## Scope Boundaries
**MVP in scope:** parent registration (Latvian), admin approval workflow, member registry, training group assignment, secure documents, OCR assist (non-blocking), Invoice Ninja billing sync, sibling discount, CSV export.

**Out of scope:** coach portal, adult members, attendance tracking, WhatsApp bot, event planning, direct FA integration.

## Skills / Workflows
- **brainstorming** — invoke before creative feature or design change.
- **writing-plans** — use when implementing multi-step work from spec.
- **test-driven-development** — required for feature and bugfix work.
- **verification-before-completion** — always run full verification before claiming done.
- **subagent-driven-development** — preferred execution mode for plan-driven work in this repo.
- **finishing-a-development-branch** — use when implementation is complete and ready for merge/PR decision.
- **uv** — always use `uv` for Python deps; never edit `pyproject.toml` manually without justification.
