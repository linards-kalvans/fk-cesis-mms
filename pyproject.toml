[project]
name = "fk-cesis-mms"
version = "0.1.0"
description = "FK Cēsis MMS — youth football club management MVP"
requires-python = ">=3.12"
dependencies = [
    "django>=5.0",
    "pytest>=9.0.3",
    "pytest-django>=4.12.0",
]

[dependency-groups]
dev = [
    "mypy>=1.20.2",
    "ruff>=0.15.12",
]
